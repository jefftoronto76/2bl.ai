// services/chat/booking.ts
//
// Booking card injection (server: build [BOOKING: ...] lines from
// sage_parameters) + parsing (pure: parseBookingCards). Scaffold only —
// implementation in a later Phase 3 commit.

export {}
