// services/chat/session.ts
//
// Session lifecycle: onFinish handling with a single batched chat_sessions
// write (Amendment 3) — token counts, calendar-offered flag, visitor-name
// capture, status — plus name extraction. Uses the pooled (PgBouncer, 6543)
// connection (Amendment 5). Scaffold only — implementation in a later
// Phase 3 commit.

export {}
