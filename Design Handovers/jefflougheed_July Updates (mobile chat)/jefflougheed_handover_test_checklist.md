# **jefflougheed.ca — Chat Widget Handover: UA / Integration Test Checklist**

*Scope: WidgetShell.tsx (Hero \+ Chat), globals.css, SectionRail.tsx, MessageActions.tsx  ·  2026-07-21*

| \# | Change | File(s) / README § | What to verify | Test type |
| :---- | :---- | :---- | :---- | :---- |
| **1** | Hero idle padding — bigger top/bottom breathing room; min-height:100dvh | globals.css §1 | At rest, hero section has \~92px top padding and 120px bottom. Page fills viewport vertically without clipping. Resize to mobile (390px): padding compresses via clamp, no overflow. | **UA** |
| **2** | Headline type scale — "Hi, I'm Jeff." scales up to 96px on wide screens | globals.css §2 | h1 renders at ≥48px at 390px viewport and approaches 96px at 1440px+. Sub-headline (.lede) scales 20–28px. .sage-line renders at 14px. Confirm no text overflow or wrapping issues at any breakpoint. | **UA** |
| **3** | Collapse × button — conditionally rendered (only when engaged), moved from top-right to bottom-right | WidgetShell.tsx §3 | Before first message: no × button in DOM (inspect). After first message: × appears bottom-right of hero. Click ×: conversation collapses, button disappears. Confirm no ghost element at top-right. | **UA \+ Integration** |
| **4** | Engaged layout — 3-row CSS grid (hero / messages / composer) replaces sticky-header approach | globals.css §4, WidgetShell.tsx | After sending first message: .stage gains .engaged class. Hero content stays at top, message thread fills middle, composer locks to bottom. No viewport scrolling of the chat surface. Confirm on 390px mobile: all three rows visible, composer not obscured by browser chrome. | **UA \+ Integration** |
| **5** | Message thread card — cream background, rounded corners, soft shadow (idle \+ engaged) | globals.css §5 | Once messages exist: .hero-conversation has visible cream card (rgb 245 244 240), 18px border-radius, faint border, shadow. Text inside card reads in dark ink — not cream on cream. Check \--color-text-primary resolves to dark inside card scope. Verify same treatment applies in .chat-overlay-log. | **UA** |
| **6** | Composer shadow \+ Sage-glow accent ring on focus | globals.css §6, WidgetShell.tsx (composer--sage-glow class) | Composer box-shadow is visibly deeper at rest. On focus/typing: subtle green accent ring appears around composer (not just a border — the glow). Confirm \--color-accent is declared as RGB components in tailwind.config.js (required for opacity modifier). If hex, glow will not render. | **UA** |
| **7** | Overlay composer — matches hero composer (SAGE·AI badge, meta row, new-conversation link) | WidgetShell.tsx §7 (WidgetShellChat), globals.css §7 | Open \#chat overlay. Bottom input bar shows: same rounded composer box, SAGE·AI badge with dot, "new conversation" link when a session exists, ↵ to send hint. Compare side-by-side with hero composer — should be visually identical. Confirm new-conversation confirm dialog works in overlay. | **UA \+ Integration** |
| **8** | Overlay chrome — custom CSS classes replace Tailwind utilities / inline styles | globals.css §7/§8, WidgetShell.tsx (WidgetShellChat) | Open overlay: header is 56px tall, has blur/glass effect, shows title \+ close button. .chat-overlay-log renders as cream card (matching §5). Greeting message centered, italic, font-display. CTA section (\#chat anchor) padding is 96px top/bottom. Confirm no residual inline-style overrides conflicting with new classes. | **UA** |
| **9** | MessageActions icon color — hardcoded dark ink escape hatch (.msg-action-ink) | globals.css §9, WidgetShell.tsx (makeRenderAssistantMessage) | After receiving an AI response: copy / regenerate / thumbs-up / thumbs-down icons are clearly visible in dark ink against the cream card. No icons fading into background. Inspect: wrapper div has class msg-action-ink. Verify same in overlay log. | **UA** |
| **10** | MessageActions at-rest opacity — 0.9 (was 0.6); no dimmed-to-bright color shift on hover | MessageActions.tsx §10, globals.css §10 | Message action icons are clearly visible without hovering. On hover: opacity goes to 1.0. Icon color does not change on hover (was text-muted → text-primary). Active states (thumbs up \= green, thumbs down \= red) still apply. Confirm both in hero thread and overlay log. | **UA** |
| **11** | SectionRail stays visible while chat is engaged or overlay is open | SectionRail.tsx §11 | Open chat (hero engage): POV · Outcomes · Principles nav remains visible. Open overlay (\#chat): rail still visible. Click a rail link while chat is open: smooth-scrolls to section. Confirm no console error from removed useWidgetShell import. | **UA \+ Integration** |

*Note: "UA" \= manual browser verification. "UA \+ Integration" \= also confirm the interaction triggers the correct state change in the chat engine (isEngaged, session, overlay open/close).*