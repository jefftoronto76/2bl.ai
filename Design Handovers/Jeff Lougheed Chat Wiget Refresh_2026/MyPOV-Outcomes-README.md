# Handoff — My POV coda on Outcomes (for Claude Code)

**Branch:** `feat/mypov-section-why` (or a branch off it) — **not `main`**.
The How I work half already shipped there; this is the missing twin.

## Files here
- **`MyPOV-Outcomes.md`** — the spec. Three-step diff: add `POV` constant, add
  `MyPovFigure`, swap the operator `<p>` for `<MyPovFigure />`. Start here.
- **`SectionOutcomes.tsx`** — the full file with the change already applied, if
  you'd rather drop it in and diff than hand-apply. Drops into
  `app/(jefflougheed)/components/SectionOutcomes.tsx`.

## Why
The earlier `MyPOV-HowIWork.md` handover wrongly said this coda was "already
shipped on Outcomes." It wasn't — so How I work got the eyebrow + avatar
treatment and Outcomes kept the bare italic line. This closes the gap so the
two twin sections match.

## Done when
Operator (Special Projects) mode on "Outcomes I focus on" shows the **My POV**
eyebrow + rule, the italic line, a **JL** avatar, and *Jeff Lougheed / Operator
& Coach* — identical to How I work. Coach mode unchanged.
