# Status — 2026-09-25

## What got done

- **Traffic Cop is genuinely live in production.** Six PRs merged tonight, in order: #495 (admin page-load phase timing, with a real Codex-caught bug fixed — a caught error was logging a fictitious HTTP status), #496 (Phase 3a cutover — `streamChat` now sends `resolveTurnPrompt`'s real output instead of the old six-segment concatenation; shadow mode kept alive, now re-running the legacy path for comparison instead of the other way around), #497 (a real preview-tenant bug — `?preview=jefflougheed` was silently resolving to the wrong tenant for API calls due to a slug mismatch with the database), #498 (admin layout no longer resolves `getAuthContext()` three times per render — down to one), #499 (Settings' Prompt Sets and Sage Parameters panels now refetch on window focus, fixing a real staleness bug — new rows weren't appearing until a manual reload), #500 (`SlotRuleConfig` is now keyed per-tenant instead of one flat object shared by every tenant — closes a real risk found before it shipped: a slot mapping added for Heirloom would have silently applied to jefflougheed.ca and Second Brain Labs too).
- **Full shadow-mode verification checklist closed for everything in scope.** 24 real shadow turns ever recorded, 100% `parity = true`, zero failures. Rows 2, 3, 6, and 7 all individually confirmed against production data before the Phase 3a cutover shipped.
- **Real production bug found, diagnosed to its exact mechanism, and documented:** invited members can end up as two separate `members` rows (Known Gaps.md, "Invited-row orphaning"). Two distinct trigger paths identified precisely — an invite ignored in favor of independent signup, and a webhook/client-sync timing race. Deliberately parked, not fixed — low current volume, real fix scoped for when invite volume increases.
- Created and published the `visitor` prompt type and prompt-set for Heirloom — currently an exact copy of `base`'s content, so this is a zero-behavior-change scaffolding step. Rewording is separate, deferred work.
- Mapped the full Traffic Cop identity/session decision tree with Jeff — the real three-way visitor/member/invited taxonomy (with real schema backing via `members.source`), what's already built (session resumption, `primer` injection) vs. genuinely unbuilt (story-invite → session-context wiring, confirmed zero rows ever in `chat_session_context`). Written into the design doc as a new top-of-file decision tree section plus Appendix C.
- Admin performance: root-caused the original "20+ second admin loads" complaint precisely — `getTenantName()`/`getTenantType()` were each independently re-running the full auth-resolution chain, concurrently, as the first database-touching calls of the request (1,400–2,000ms each in real data, vs. 3ms for the same resolution done once). Fixed in #498.

## What didn't get done

- **Phase 4 — base-prompt becomes slot-aware.** The per-tenant config from #500 exists, but `base-prompt.ts` still ignores it entirely — grabs whichever compiled row has the newest version, regardless of type. This is the next real step; nothing from tonight's slot work affects real prompt content yet, only the decision record.
- Sprint 1 proper (`memberStatusSlots.member`) — still unset for every tenant. Only `.visitor` is wired, Heirloom only.
- The actual "Start Your Story" trigger — designed and agreed (fire a real hidden turn via the existing `sendHidden` mechanism, using the already-written "Opening Greeting" block), never built.
- Date/time context provider — designed, real dependency identified (needs the client to send timezone, not currently wired), not built.
- Member-context field expansion (session count, last visit) — designed, not built.
- Rows 1 (story-scoped) and 5 (invite-holder) — deliberately excluded from tonight's shadow-verification gate. Real, live, built code paths, never shadow-tested. Accepted trade-off, not an oversight — full reasoning in Chat Server.md's "Gate result" note.
- `(platform)/layout.tsx` has the identical triple-auth-resolution bug #498 fixed for the admin layout. Flagged, not fixed — real follow-up.
- Possible second instance of #497's bug: `?preview=sbl` forwards `'sbl'`, while the real tenant slug may be `'second-brain-labs'`. Flagged by CC, never confirmed against the database, never fixed.
- Sign-up/sign-in identity test sheet — still 0 of 18 rows.
- Security/RLS P0 items — not touched tonight.

## What we're doing next

- Real manual verification once production has redeployed tonight's merges: confirm an anonymous Heirloom turn shows `selection.slotKey = 'visitor'`, and confirm jefflougheed.ca still shows `default-slot`/`base`, completely unaffected.
- Pull fresh `ADMIN_PAGE_LOAD_TIMING` data after a few real admin loads on the now-deduped layout — compare `metadata.totalMs` before/after, not the per-phase numbers (there's a real memoization caveat noted in #498's own PR: the old "3ms" reading may not reflect the true cost of a cold auth resolution).
- Build Phase 4, targeting the per-tenant shape #500 already established.
- Build the "Start Your Story" trigger.

## Anything else that matters

- The Traffic Cop design doc (`Design Handovers/september_2026/traffic_cop_design_2026-09-05.md`) now has a plain-language decision tree at the top and a full Appendix C with tonight's identity/session findings — read that before re-deriving any of this from scratch.
- `System Docs/Known Gaps.md` has the full, precise writeup of the invited-row orphaning bug, including which two mechanisms cause it and which one is confirmed vs. inferred for the two real cases found.
- Everything from tonight was verified directly against real code, real production data, or both before being reported as done — not taken on trust from any single report.
