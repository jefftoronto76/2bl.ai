# Public Site (Visitor)

## Public Site (Visitor)

Public-facing components live in `app/(jefflougheed)/components/` and the shared
widget shell (`components/shells/widget/`). Tailwind + inline styles (no Mantine
on the public side).

| Component | File | Purpose |
|-----------|------|---------|
| `WidgetShellChat` (Sage overlay + `#chat` anchor section) | `components/shells/widget/WidgetShell.tsx` | Full-viewport visitor chat overlay plus the in-page `#chat` anchor section that CTAs into it. Mounted from `app/(jefflougheed)/page.tsx`; toggled via `useWidgetShell.expand()` / `collapse()`. **Overlay** is a `fixed inset-0` wrapper around an inner `flex min-h-0 flex-col`. **Prior approach (superseded 2026-07-22/23, commits `0990067f`/`867e46fe`, PR #217):** the inner wrapper was `flex h-dvh min-h-0 flex-col` — height was pure CSS (`h-dvh`), unconditionally, no JS-computed inline height or transform; `app/layout.tsx`'s `interactiveWidget: 'resizes-content'` viewport export made `h-dvh` shrink correctly on keyboard-open on both iOS Safari and Android Chrome; `useKeyboardViewport` was called with `trackViewport: false`, used only for its `lockBodyScroll: true` side effect (freezes the document with `position: fixed` + `top: -scrollY` on open, restores scroll position via `window.scrollTo` on close — hardened beyond `overflow: hidden`, which iOS ignores during focused-input auto-scroll); no `visualViewport` listener ran and no height/transform was computed in this component (mirrored `components/shells/membership/ChatHero.tsx`'s overlay fix at the time). **Current behavior:** height is `var(--vvh, 100dvh)` inline-styled on the wrapper, kept live by a dedicated `visualViewport.resize` listener in this component — JS-computed, not pure CSS. `useKeyboardViewport` is still called with `trackViewport: false, lockBodyScroll: true` for the scroll-lock side effect only. See `System Docs/jefflougheed Chat Widget.md`'s §3 (hook contract) and §4 (`--vvh` mechanism, including this full history) for more. The composer is a `sticky bottom-0` last child inside the same `overflow-y-auto` messages container, not a flex sibling after it (composer-inside-scroll-container pattern) — `role="log"`/`aria-live`/`aria-busy` are scoped to the message-content wrapper only, not the composer, so the composer's own content changes never fire a live-region announcement. Compact 56px header (`h-14`, `bg-bg/90` backdrop-blur) with a status pip (`h-1.5 w-1.5 rounded-full`, `bg-accent` when `isStreaming`, `bg-accent/35` otherwise) next to a Playfair 17px wordmark; 44×44 SVG close button (48×48 invisible hit-area via a `before:` pseudo-element). Assistant messages render through `SageReply` — typographic block with a 2px `border-accent/35` left rule, `pl-4`, Playfair 18px prose, `max-w-[680px]`, `sage-slide-up` entry animation; **no bubble** (no bg, no border-radius, no shadow). **Visitor bubble (converged with Heirloom's structure 2026-07-28, per `Design Handovers/spec_visitor_bubble.md`; widened + alignment-fixed same day):** `w-fit max-w-[90%]` (shrink-to-fit measure, was a fixed `max-w-[560px]`, then `76%` — widened to `90%` after the 76% measure read as cramped next to the assistant's full-width response; `EditableUserBubble`'s editing-state measure widened in step, `86%` → `95%`, to stay proportionally roomier than the read state), `rounded-[18px] rounded-br-[5px]` (18px radius with a 5px bottom-right tail), `border` + `bg-surface`/`border-border` (was borderless on the sent state) — these resolve to jefflougheed's own cream-on-white fill via this file's own CSS cascade, not Heirloom's literal palette — see `System Docs/jefflougheed Chat Widget.md`'s §5 for exactly which selectors carve this back from the site's dark default theme, including the selector gap that once let the overlay's own message log render dark instead (fixed 2026-08-06/07). **Shrink-to-fit-vs-percentage bug (found in production 2026-07-28):** the `<p>` bubble used to sit inside an extra `<div className={status === 'failed' ? 'chat-bubble-shake' : undefined}>` wrapper (added only to conditionally apply the failed-state shake animation). That wrapper was a second, unnecessary level of shrink-to-fit sizing between the bubble and its `items-end` flex-item parent — a percentage `max-width` on a plain block descendant of an `align-items:flex-end` flex item (rather than on the flex item itself) hits an indeterminate/circular CSS sizing case, and Chrome resolved it to a width far narrower than the content needed. Confirmed live in production and reproduced on a local dev build via Playwright driving the real composer (not a synthetic harness): typing "Hello" computed the bubble to ~72×76px and wrapped it into two lines ("Hel"/"o") despite ~960px of available width. Fix: apply `chat-bubble-shake` directly on the `<p>` itself (conditionally, alongside its other classes) and drop the wrapper `<div>` entirely, so the bubble is the direct flex item — re-measured at ~80×51px, single line, same content. Same fix applied to `MessageList.tsx`'s bubble `<div>` below, which had the identical wrapper pattern. Text is `font-body` (DM Sans) at 15.5px/1.62, `text-left` (was `font-display` Playfair italic at 18px right-aligned — the font was a real token violation, Playfair is the display font not body; `text-left` fixes wrapped multi-line text reading right-flush, which the spec calls out as wrong). Failed state is `bg-red-400/10 border-red-400/45` (was border-only). The `.sage-visitor-msg` class (defined in `app/(jefflougheed)/globals.css`, not the shared `app/globals.css`) is kept — applies CSS curly quotes via `::before` / `::after` — the message content string itself never contains the quote glyphs — and `italic` is kept too: both are deliberate jefflougheed-specific decorative flourishes layered on top of the shared structure, not something the spec convergence removed. **Bubble/action-row alignment:** the per-message wrapper is `group flex flex-col items-end gap-1.5` — Flexbox `align-items: flex-end` right-aligns each child (bubble, `DeliveryStatus`, `UserMessageActions`) to the container's own edge independently of that child's own width, so the action row's right edge always lands under the bubble's regardless of which one is wider. (A same-day CSS Grid version of this — `group grid justify-end gap-1.5` with `DeliveryStatus`/`UserMessageActions` wrapped in `w-full min-w-0` divs — was tried and reverted: a single-column CSS grid defaults every item to `justify-self: stretch`, and the shared column is sized to the WIDEST row's max-content, usually the actions row, not the bubble — so the bubble's own `max-w-[90%]` resolved against the actions row's width instead of the real container, and for a short message the two rows' right edges were measurably ~136px apart, not aligned at all. Confirmed via a Playwright/Chromium harness reproducing the exact DOM+CSS before reverting.) Mirrors `components/shells/membership/MessageList.tsx`'s identical structure. **Visitor message actions (added 2026-07-27, full parity as of the same date):** each visitor message renders `components/chat/UserMessageActions.tsx` — full Edit/Copy/Send again, same as Heirloom — once `status` is `'sent'`, plus `components/chat/EditableUserBubble.tsx` swapped in place while editing (local `editingId` state, cleared if the target message disappears). Initially shipped Copy-only (`showEdit={false} showResend={false}`) because editing/truncating history here could silently discard an already-offered booking card or an already-captured NAME/EMAIL/PHONE with no undo; flipped to full parity once `conversion_events` landed (see `System Docs/Database Schema.md`'s `conversion_events` row + `services/crm/conversion-events.ts`) — truncation now flips any still-`'presented'` booking/contact event to `'overwritten'` instead of just vanishing, so the discard is tracked rather than silent. `editMessage`/`resendMessage` come from `useChatSessionContext()`, same shared engine as Heirloom. Empty state **is** the greeting: mode-aware Playfair copy ("Hi, I'm Sage. *What brings you here?*" default, "Ask me anything about *Jeff's work*." question-mode) in the same left-rule card style. First user send populates `messages` and the empty state unmounts. There is no `sendGreeting` — `messages.length === 0` is the canonical greeting state. Composer markup (`.chat-overlay-composer`/`.composer`/`.send`, hand-written CSS in `app/(jefflougheed)/globals.css`, not Tailwind utility classes) has `padding-bottom: calc(12px + env(safe-area-inset-bottom))` on mobile; send/stop button is a 38×38 accent-filled circle (`.composer .send`) with a `::before{inset:-5px}` pseudo-element giving a 48×48 invisible hit-area; textarea is borderless/transparent inside the `.composer` wrapper's own `border-radius:18px` card. The streaming indicator uses three dots with the `sage-pulse` keyframes and carries `data-sage-streaming` for the reduced-motion guard. Reduced motion is honored at the CSS layer via `@media (prefers-reduced-motion: reduce)` disabling animations on `.sage-animate`, `.sage-visitor-msg`, and `[data-sage-streaming] > *`. The two remaining explicit inline `style` props in the overlay (`isExpanded`) JSX are the `--vvh`-driven height noted above and the per-dot `style={{ animation: 'sage-pulse ...' }}` on the streaming indicator — most of the rest is Tailwind, though the composer/greeting/visitor-bubble markup described above uses hand-written CSS classes (`.composer`, `.chat-overlay-composer`, `.chat-overlay-log`, `.sage-visitor-msg`, etc. in `app/(jefflougheed)/globals.css`) rather than Tailwind utilities (`markdownComponents` and `BookingCard` internals also excepted). The separate, always-rendered `#chat` anchor section below uses its own extensive inline `style={{}}` props, not Tailwind at all. The overlay container sets **no inline color tokens** — it inherits the palette from `html[data-brand="jefflougheed"]`. (The attribute was itself renamed from `data-palette="inkwell"` on 2026-06-23, commit `6fab7343` — a couple of other `System Docs/` files still use the old name; see `System Docs/jefflougheed Chat Widget.md`'s §8.) Assistant prose color resolves through `var(--color-text-primary)` via `markdownComponents` (`components/shells/widget/sage/markdownComponents.tsx`), which uses palette tokens rather than hardcoded hex. Reads `mode` from the shared session (`useChatSessionContext`); the shell open/close + question `mode` live in `useWidgetShell`. On mount, `detectModeFromLocation()` parses `?mode=question` from the hash-query or top-level search string and — if `'question'` — calls `expand('question')` to auto-open the overlay. `mode` is read by the `useChatTurn` engine (`services/chat/ui/v1/`) via injected `ChatEngineAccessors` and sent to `/api/sage` on every send/retry so the server appends the question-mode CONTEXT block — the send → create-session → stream → PATCH lifecycle lives in the hook, not in this component. Marker parsing is documented separately in `System Docs/Marker Syntax.md`; `SageReply` resolves each parsed card to a `sage_parameters` row by URL match and spreads `openAs` / `embedCode` into `<BookingCard>`. **`#chat` anchor section** (same file) renders: eyebrow "Not Sure Yet?" → headline → lede → green Start/Continue CTA → outlined Book a Session link. The inline 240px transcript preview that used to render when `messages.length > 0` has been removed; the CTA label still toggles "Start a Conversation" / "Continue Conversation" based on `messages.length`. Reveal animation on the anchor section is unchanged. |
| `WidgetShellHero` | `components/shells/widget/WidgetShell.tsx` | **Chat-first hero** — the landing headline ("Hi, I'm *Jeff*."), lede, and `sage-line` sit above an **inline chat surface** (composer + conversation canvas + suggestion chips) in the same `#hero` section. It is a standalone inline chat that **shares conversation state with the `WidgetShellChat` overlay** via the shared session (`useChatSessionContext`, instanceKey `"sage"` — the two surfaces drive one conversation) but does **not** use `expand()`. The turn engine is the shared session's `useChatTurn` (owned by the single `ChatSessionProvider`); `submit()`, Enter-to-send (`onKey`), and the suggestion-chip `handleChipClick` all call `session.send(text)`, the error block's Retry calls `session.retry()`, and `session.errorType` (null when the last turn succeeded, else the classified `ChatErrorType` — see `System Docs/Utilities/Chat UI.md`'s `useChatTurn.ts` row for the classification) drives that block. Only `setComposerRef` is shell state (`useWidgetShell`). `isStreaming` (session) drives the three-dot `sage-pulse` indicator and disables the composer/chips. Messages render like the overlay: visitor as right-aligned italic Playfair `.sage-visitor-msg`, assistant via `SageReply` (booking cards parsed by `parseBookingCards`, params from `useSageParameters`). `isEngaged = messages.length > 0 && conversationVisible` toggles the `.stage` → `.stage engaged` class; a `close-x` button collapses the canvas (`conversationVisible`), and the 5 suggestion chips render only when not engaged. On mount it registers the textarea via `setComposerRef` and runs `detectModeFromLocation()` — `?mode=question` (hash-query or top-level search) → `setMode('question')` + focuses the composer. **iOS keyboard handling. Prior approach (superseded 2026-07-22, commit `0990067f` — "remove JS keyboard handling"):** the shared `useKeyboardViewport` hook (no scroll-lock — deliberate; `position: fixed` on the body breaks iOS keyboard detection in an inline context) fired `onViewportChange` on every `visualViewport` resize/scroll event, writing `--kb-surface-h` and `--kb-surface-y` CSS vars onto `chatSurfaceRef` and returning `keyboardOpen`; `keyboardOpen` flipped `.chat-surface--kb` (mobile only — the viewport height never drops below the threshold on desktop, so layout was untouched). The CSS vars drove a fixed-position surface overlay (height + compositor `translateY`) so the composer sat directly above the keyboard without needing `position: fixed` on the surface itself. (This itself replaced an even earlier all-CSS-var height write, `--kb-surface-h` alone without the `-y` offset var — see commit `4c5ba1ae`, 2026-07-15, "Converge keyboard-viewport consumption across surfaces onto `--vvh`," if tracing further back matters.) **Current behavior:** mobile no longer uses any JS-driven keyboard handling for this surface at all — `.stage.engaged .chat-surface` becomes a pure-CSS `position: fixed; inset: 0` full-screen takeover instead (`app/(jefflougheed)/globals.css`), and the shared `useKeyboardViewport` hook is called with `lockBodyScroll: false, trackViewport: false` — no CSS-var writing, no keyboard-driven class toggle. See `System Docs/jefflougheed Chat Widget.md`'s §3 for the hook's full contract and why this inline surface must never lock body scroll, §4 for the fuller history of this mechanism's evolution, and §2 for the mobile-only composer-tap routing mechanism (`handleComposerPointerDown`) that means this component's engaged state is rarely reached by real mobile visitors at all. **Auto-scroll (fixed 2026-07-28):** `ChatThread`'s `scrollGuard` is `() => conversationVisible && messages.length > 0` — restored after a `DIAGNOSTIC (temporary)` comment had it hardcoded to `() => false` (commit `dbb7a08`, predates this fix by several days and is unrelated to any of the visitor-bubble work). With auto-scroll disabled, `.hero-conversation`'s `overflow-y: auto` never scrolled itself, so once a conversation grew past the visible height, new messages rendered below the scroll container's clipped bottom edge and were invisible until manually scrolled — confirmed via Playwright (`heroConversation.scrollTop` stuck at `0` despite `scrollHeight > clientHeight`) and a screenshot showing message 6 of 6 simply absent, no z-index/stacking involved. `WidgetShellChat`'s overlay was never affected — its own `scrollGuard={() => isExpanded}` (a different prop, same `ChatThread`) was untouched. Verified safe to re-enable on mobile specifically: a real tap on Hero's composer never reaches this code path at all (see `handleComposerPointerDown` below — it redirects to the overlay before focus), and the residual path (keyboard/assistive-tech focus, which isn't intercepted by the pointerdown guard) was tested directly at a mobile viewport width and scrolls correctly. |
| `Nav` | `app/(jefflougheed)/components/Nav.tsx` | Top fixed navigation for jefflougheed.ca. Three entries from a `LINKS` array: `Book` (`kind: 'scroll'` → smooth-scrolls to `#how-it-works`), `Labs` (`kind: 'external'` → `https://www.2bl.ai`), and `Share` (`kind: 'share'` → opens `ShareModal`). Desktop renders the links inline; mobile uses a hamburger that toggles a fixed dropdown. Scroll past 60px (or an open mobile menu) swaps the bar to a blurred translucent background with a bottom border. **No chat coupling** — Nav does not import `useWidgetShell`/the session and never calls `expand()`. Its only cross-module dependency is `ShareModal`, imported via relative `./ShareModal` now that Nav sits alongside it in `app/(jefflougheed)/components/` (the old `src→app` boundary warning is cleared). Styles are inline + a small `<style>` block (no `app/globals.css` `nav-chat-*` classes). |

### Discovery-call link cleanup

The free 15-minute discovery call is removed as a **user-facing link**
on the public site. The discovery call is **not** removed from Sage's master
system prompt; Sage may still offer it at her discretion during a
conversation. **Stale as of this pass:** `services/prompt/sage-prompt.ts`'s
`DEFAULT_SYSTEM_PROMPT` no longer holds the pricing/behavior/booking-link
prompt content — it's now a one-line generic "I'm having a technical issue"
fallback string, used only when a tenant has no live `compiled_prompts` row
(`services/prompt/compiler.ts`'s `getSystemPrompt`). The real master prompt
is admin-editable, block-based content (`blocks` table, assembled by
`services/prompt/compile.ts`/`composer.ts` into `compiled_prompts`) — there
is no single static file to point to for the pricing/booking-link sections
anymore. Remaining intentional references:

- `app/(jefflougheed)/components/Session.tsx` — `handleDiscoveryClick` popup invocation and the "Start with a free 15-minute call →" link

Do not remove these without explicit instruction.

### Heirloom storefront (chat)

The Heirloom chat is the platform's **membership shell** (the chat IS the
product — a slide-in modal panel; see "Chat shells" in README). As of
centralization Step F its presentation lives in **`components/shells/membership/`**
(Tailwind + the `[data-brand="heirloom"]` palette — no Mantine, isolated from the
Sage visitor chat in `components/shells/widget/`), app-importable shared presentation that
`app/heirloom/page.tsx` mounts. It keeps its own `useReducer`-backed shell store
(`chatStore.tsx` here; the pure `chatReducer.ts` is headless in
`services/chat/ui/v1/`), but its turn engine is the **shared `useChatTurn` hook**
(`services/chat/ui/v1/`, PR #44) — the same engine jefflougheed consumes. The
Heirloom-local `app/heirloom/lib/stream.ts` reader was deleted; the hook owns
transport via the shared `readDataStream` (`services/chat/server/stream-utils.ts`).
`app/heirloom/` now holds `page.tsx` (server gate), `HeirloomApp.tsx` (client shell),
`layout.tsx`, `globals.css`, and `components/landing/*`.

| Component | File | Purpose |
|-----------|------|---------|
| `HeirloomPage` (server gate) | `app/heirloom/page.tsx` | **Server component** — reads `invite_gate_enabled` from `tenants.settings` JSONB for `HEIRLOOM_TENANT_ID` (default `true` when key absent) and determines `isAuthorized` via two paths: (1) signed-in Clerk user with an `active` members row; (2) valid unused `?invite=TOKEN` query param (checked via `validateMemberToken()` from `services/members` — reads `members.token` / `members.used_at`, no mark-used at render time). If either path resolves, `isAuthorized = true`; when a valid token resolves, `invitedName` is extracted from the members row, along with `invitedEmail`/`invitedPhone` (added 2026-08-14, PR #372 — `validateMemberToken()`'s select and the `MemberInviteRow` interface were missing `phone` until this fix, despite `createMemberInvite` already writing it) — all three pre-fill `MagicLinkCard`'s sign-up form (see the `chatStore` row below) when no `[NAME:]`/`[EMAIL:]`/`[PHONE:]` marker has fired yet. **A third, independent path (2026-08-10, reusable-story-invite-links):** a valid `?join=TOKEN` (checked via `validateStoryInviteToken()` from `services/crm/story-invites` — reads `story_invite_links`, a wholly separate table/token from `members.token`, zero shared code with the `?invite=` path above) also sets `isAuthorized = true`. Unlike `?invite=`, this branch is **not** gated on `!isAuthorized` and the resulting `storyInviteToken` is passed through to `<HeirloomApp>` regardless of whether a `session` already exists — an already-signed-in existing member opening a `/join/[token]` link is already `isAuthorized` from path (1) above, but the client still needs the raw token to fire the accept call (see the `chatStore`/`ChatProvider` row below for its two accept triggers). **This branch also sets `autoOpenChat = true` (added 2026-08-10)** — originally `autoOpenChat` only ever came from `members.auto_open` on the `?invite=` path above, so a story-invite visitor landed on a closed chat panel. **As of 2026-08-13 (PR #367) the `?invite=` path no longer reads `members.auto_open` either** — it now sets `autoOpenChat = true` unconditionally for any valid admin/member invite token, so both paths always auto-open the chat regardless of that column's value (kept only for `InviteMemberModal.tsx`'s toggle UI — see `System Docs/Known Gaps.md`). **Resolves `storyInviteStoryTitle` / `storyInviteInviterName` (added 2026-08-11)** — plain display strings queried from `artifacts.title` (the link's `story_id`) and `members.name` (the link's `created_by`) right alongside the `validateStoryInviteToken()` call, never fed to a model as an instruction; passed through to `<HeirloomApp>` alongside `storyInviteToken` and consumed only by `chatStore.tsx`'s one-time contextual greet (see that row below) — `null` when either lookup comes back empty, in which case the greet effect falls back to its plain "Hi". Passes `gateEnabled`, `isAuthorized`, `invitedName`, `invitedEmail`, `invitedPhone`, `hasInviteToken`, `storyInviteToken`, `storyInviteStoryTitle`, `storyInviteInviterName`, and (when the visitor was authorized via an unused `?invite=` token) the raw `inviteToken` string to `<HeirloomApp>`. The raw `inviteToken` is passed only on that token-auth path — not to already-signed-in members; `storyInviteToken` follows the different rule above. The landing page always renders; the gate only affects the chat widget interior. **`tokenExistsButUnauthorized` + chat-first expired-invite bypass (added 2026-08-14):** right after the `?invite=` validation block, a new `memberTokenExists(inviteToken, tenantId)` check (`services/members/members.ts` — existence only, no `used_at`/`revoked_at`/`status` filter, but scoped to `tenant_id` unlike `validateMemberToken`) answers "was this token ever genuinely issued for this tenant?" independently of `hasInviteToken`, which is raw query-param presence with zero validation. When true **and** `!session`, `autoOpenChat` is also set (previously `false` for this population — nothing opened the chat panel for an invalid/expired token at all). `tokenExistsButUnauthorized` is passed through to `<HeirloomApp>` alongside the props above; see the `chatStore` row below for what it drives once there. **GateView** when `!hasInviteToken` shows `WaitlistView` (email form → `POST /api/heirloom/members/waitlist`); when `hasInviteToken` but the token doesn't exist for this tenant at all (garbage/unrelated string) also still shows `GateView`'s static "By invitation only." UI, gate intact. When the token genuinely exists for this tenant but didn't authorize (expired/used/revoked/cross-tenant is excluded by the tenant scoping above) and the visitor isn't signed in, `GateView` is bypassed entirely in favor of the chat-first flow (see `chatStore`/`GateView` rows below) — it no longer opens a Clerk sign-up modal for this case. |
| `HeirloomApp` | `app/heirloom/HeirloomApp.tsx` | `'use client'` shell — accepts `gateEnabled` and `isAuthorized` from the server page, wraps `<ChatProvider gateEnabled={…} isAuthorized={…}>`, and renders the landing page + slide-in chat panel. Escape key and backdrop click dispatch `CLOSE_CHAT`. Panel carries `role="dialog"` / `aria-modal` / `aria-hidden`. |
| `chatStore` (`ChatProvider`, `useChatStore`, `Message`) | `components/shells/membership/chatStore.tsx` | `useReducer` context composed with the shared session (`useChatSession`, `services/chat/ui/v1/core/`). **Stale note (2026-07-27): the reducer actions below no longer exist as such.** Conversation state (`messages`/`sessionId`/streaming/error) now lives entirely in the shared `useChatSession` store; the local `useReducer` (`chatReducer.ts`) owns only shell state (`TOGGLE_SIDEBAR` / `SET_SIDEBAR` / `OPEN_CHAT` / `CLOSE_CHAT`) — the rest of this entry predates that migration and is kept for its still-accurate parts (gate props, persistence, DB recovery). **Gate props**: `ChatProvider` accepts `gateEnabled?: boolean` (default `true`), `isAuthorized?: boolean` (default `false`), `invitedName?`/`invitedEmail?`/`invitedPhone?: string | null` (default `null` — the admin-set values from the invite row, exposed on `ChatContextType` and read by `MessageList.tsx` as a `MagicLinkCard` pre-fill fallback, see that row below; `invitedEmail`/`invitedPhone` added 2026-08-14, PR #372), and `inviteToken?: string` (the raw invite token, present only on the token-auth path — stored in a stable ref and consumed once on the false→true `isSignedIn` transition by `acceptMemberInviteToken` (added 2026-08-13, PR #367) to call `POST /api/heirloom/invites/accept` — same shape as `acceptStoryInviteToken` below: fire-once ref (`memberInviteAcceptFiredRef`), `res.ok` check, and an `injectAssistantMessage` fallback ("Something went wrong setting up your account…") on failure or a rejected fetch; previously a bare fire-and-forget fetch with no such handling). **`storyInviteToken?: string` (added 2026-08-10, reusable-story-invite-links) — a wholly separate mechanism, zero shared code with `inviteToken` above:** also stored in a stable ref, but consumed from **two** independent trigger points, not one — the same false→true `isSignedIn` transition (brand-new signup, firing alongside but not instead of the `inviteToken` call above) **and** a second, new mount-time effect that fires as soon as `isLoaded` is true if the visitor was *already* signed in (an existing Heirloom member opening a `/join/[token]` link while already logged in never crosses a sign-in transition, so without this second trigger the accept call would simply never fire for that case — the specific gap this feature's investigation found). Both triggers call `POST /api/heirloom/story-invites/accept` (guarded by a `storyInviteAcceptFiredRef` so only one ever actually fires) and, on success, set a new `joinedStoryConfirmation: { title: string } | null` context field. **`storyInviteToken` also exposed on `ChatContextType` and threaded to `MagicLinkCard` (added 2026-08-11, PR #351) — a second, independent consumer of the same ref, not a replacement for the two triggers above:** `MessageList.tsx` reads it off context and passes it into `<MagicLinkCard storyInviteToken={...}>` alongside the pre-existing `inviteToken` prop; `MagicLinkCard` → `useAuthFlow`'s `sendEmail`/`sendPhone` → the Clerk adapter (`services/auth/providers/clerk/client.ts`) write it into Clerk `unsafeMetadata` as `heirloom_story_invite_token`, combined into the SAME `signUp.update()` call the pre-existing `inviteToken`/`heirloom_invite_token` write already makes (`unsafeMetadata` is a full-object replace, not a merge — writing them in two separate calls would let one silently wipe the other). This lets the `user.created` Clerk webhook (`app/api/webhooks/clerk/route.ts`) call `acceptStoryInvite` directly and become authoritative for a brand-new signup, closing a real gap the two client-only triggers above could not close on their own: if the webhook happened to process before either client trigger fired, it previously had no story-invite awareness at all and fell through to a generic `syncMember` upsert, leaving `source`/`primer` unset even though access still granted correctly once the client call landed after. See `System Docs/Known Gaps.md`'s webhook-race entry and `System Docs/Utilities/Auth.md` for the full mechanism — consumed by `ChatHero.tsx` to fire a one-time "You've joined" toast, the same weight as its existing "Deleted"/"Starred" toasts. **Diagnostic logging (added 2026-08-10, story-invite-client-logging, PR #341):** the flow previously only logged on failure (`console.error`), leaving no client-side visibility into where it broke; five `[heirloom/chat]`-prefixed `console.log` checkpoints now cover the whole chain — a one-time mount effect logs whether `storyInviteTokenRef` actually received a token from the server→client handoff (truncated `token.slice(0, 8) + '…'`, the same convention as `join/[token]/route.ts` and the accept routes' own server-side logs); the mount-time effect (the "already signed in" trigger, described above) logs the evaluated `isLoaded`/`isSignedIn`/token-presence values and which of its three branches it took; the sign-in-transition effect's story-invite block logs that it was reached and whether a token was present when it fired; `acceptStoryInviteToken` itself logs immediately before the fetch fires, logs on the previously-silent success path (including the returned `story_title`), and logs when `storyInviteAcceptFiredRef` catches a second call instead of silently returning (confirming both triggers fired, not just one). Additive only — no existing error logging changed, no behavior change; per this codebase's own logging-convention rule (`CLAUDE.md` §6, prefer `audit_events` over `console.log`), these are `console.log` by deliberate exception because they trace client-side ref/effect state `audit_events` (a server-side table) can't observe without adding tracing-only API calls, and the task explicitly matched this file's pre-existing `console.error` convention — flagged at implementation time, not silently deviated. This instrumentation does not persist past the browser session or Vercel's log retention window; it is a debugging aid, not an audit trail. **Contextual auto-greet + account-creation prompt (added 2026-08-10/11, story-invite-first-run):** the one-time auto-greet effect (`autoGreetSentRef`, fires once `autoOpenChat` + `isLoaded`, deferred 350ms for hydration to settle) previously always sent a bare `sendHidden('Hi')` — a real LLM round-trip — regardless of why `autoOpenChat` was true. When a story invite is present and both `storyInviteStoryTitle`/`storyInviteInviterName` resolved (see the `HeirloomPage` row above), it now instead sends a fixed, deterministic message via `injectAssistantMessage` (`"<inviter> invited you to their story '<title>'. I'm going to help you get set up."` — no API call, since the wording never varies) and falls back to the old `sendHidden('Hi')` whenever no story invite is present or its title/inviter didn't resolve. Immediately after that greeting, in the same effect, a not-signed-in visitor (`isSignedInRef.current === false`) also gets a second injected message carrying `[ACCOUNT_CREATE: story invite]` (see `System Docs/Marker Syntax.md`), so `MagicLinkCard` renders right away — a signed-in visitor gets only the greeting, since the mount-time effect below already fires `acceptStoryInviteToken` for that case. `injectAssistantMessage` itself was changed to write `messagesRef.current` synchronously (previously only reassigned on React's next render) specifically so these two back-to-back calls in the same tick compose correctly instead of the second one silently overwriting the first with a stale pre-greeting snapshot. **`acceptStoryInviteToken` failure fallback (added 2026-08-11):** both failure paths — a non-ok response and a rejected fetch — previously only `console.error`'d, leaving the visitor signed in/signed up with no story and no explanation. Both now also call `injectAssistantMessage` with one generic, warm fallback message ("Something went wrong adding you to this story…") — deliberately not branching on the real server error string (developer-facing, not end-user copy); the `console.error` calls are unchanged, additive not replaced. **`joinedStoryConfirmation` also now triggers a stories refetch (added 2026-08-11):** previously `ChatHero.tsx`'s `stories` list was fetched exactly once, on mount, so a newly-granted story didn't appear in the sidebar until a manual page reload — the effect that fires the "You've joined" toast (see the `ChatHero` row below) now also calls a `refreshStories` callback (the same mount-fetch body, extracted so both call sites share it) alongside that toast. **The one-time auto-greet effect's if/else-if chain now has three branches, not one (as of 2026-08-14):** after the story-invite branch described above, an **admin/member invite** branch (`!storyInviteTokenRef.current && inviteTokenRef.current && !isSignedInRef.current`, present since 2026-08-13 but previously undocumented here) sends a name-personalized greeting when `invitedName` is set (`"Hi <name>, welcome! I'm going to help you get set up."`, else a generic `"Hi! I'm going to help you get set up."`) followed by `[ACCOUNT_CREATE: admin invite]` via `injectAssistantMessage` — same deterministic, no-LLM pattern as story invite. A third **expired invite** branch (`tokenExistsButUnauthorized && !isSignedInRef.current`, added 2026-08-14) then covers the case the `HeirloomPage` row above describes: a genuinely-issued-but-now-invalid `?invite=` token. No `invitedName` to personalize with (an unauthorized token carries no admin-set name) and no specific story/inviter to reference, so the greeting is generic: `"Looks like that invite link didn't work — but I can still get you set up."` followed by `[ACCOUNT_CREATE: expired invite]`. Only when none of the three branches match does the effect fall back to the original `sendHidden('Hi')` real LLM round-trip. **`isGated` formula updated (2026-08-14):** now `gateEnabled && !isAuthorized && !bypassGateForExpiredInvite`, where `bypassGateForExpiredInvite = tokenExistsButUnauthorized && isLoaded && !isSignedIn` — gated on `isLoaded` so there's no hydration-timing flash of ungated chat before Clerk's sign-in state is actually known, and on `!isSignedIn` so a signed-in-but-still-pending member with a stale invite link still gets `GateView`'s "already signed in" branch rather than falling through to the chat/greet-catch-all. Exposed on context — consumed by `ChatHero` to render `GateView` instead of the chat, and also gates `ChatInput`'s send path and `handleSelectPrompt` (see `ChatHero` row below) since those only ever mount/fire when `!isGated`. `sendMessage(content)` is the shared engine's `useChatTurn().send`; the context also exposes `retry` / `stop` / `regenerate` / `setActiveVersion` and — added 2026-07-27 — `editMessage` / `resendMessage` (Edit/Copy/Send again on visitor messages, see `System Docs/Utilities/Chat UI.md`'s `useChatTurn.ts` row), plus the engine's `errorType` (see that same row for the classification). **IndexedDB buffering** (best-effort durability in front of the DB): a `persistCurrent` callback writes the live transcript via `bufferThread` (`services/chat/ui/v1/persistence.ts`) on each turn boundary — user send and stream finish (`setStreaming(false)`); **not** per streamed token. `setSessionId` calls `clearDraft()` then re-buffers under the real session key. A mount effect rehydrates the most-recent buffered thread (`findMostRecentThread`) via `HYDRATE`, syncing the engine refs so the next turn continues the same session (DB write path untouched). A further effect flushes the live transcript on `pagehide` / `visibilitychange` (`document.visibilityState === 'hidden'`) so an interrupted turn that never reached the DB still recovers. **Navigate-away warning**: a `beforeunload` listener (bound once on mount, removed on unmount) triggers the browser's generic leave dialog when a turn is in flight (`isLoadingRef`, a ref mirror of `isLoading` set in `setStreaming`) **or** any conversation exists (`messages.length > 0`). The condition is deliberately broad — anonymous visitors have no cross-device DB recovery yet, so an existing thread is treated as unsaved on leave; tighten to a dirty/confirmed-flush check once signed-in DB recovery is proven. Both `preventDefault()` and `returnValue = ''` are set (Chrome 119+ requires both to show the dialog). **Signed-in DB recovery**: when a Clerk user is signed in (`useUser`), a further effect fetches `GET /api/sessions`, populates `recentSessions` (exposed on the context for the Recent sidebar) and — most-recent-wins — hydrates from the newest DB session only when its `updated_at` is strictly newer than the local buffer's `updatedAt` (so a fresher local thread is never clobbered). `loadSession(id)` (also on the context) loads a previously-fetched session into the conversation via `HYDRATE`. Anonymous users skip this entirely (IndexedDB only). `newChat()` (also on the context, backing the Sidebar "New Chat" button) captures the active `sessionIdRef` value, clears the engine refs (`messagesRef` / `sessionIdRef` / `assistantPendingRef` / `isLoadingRef`), drops the active thread's IndexedDB entries — `clearDraft()` for the pre-session draft slot **and** `clearSession(clearedSessionId)` for the current session-keyed entry — then dispatches `RESET`. Clearing the **session** entry (not just the draft) is what actually stops the next mount's rehydration (`findMostRecentThread`) from reloading the just-cleared conversation on refresh: once a conversation has had a completed turn it lives under its real session key, so `clearDraft()` alone left it behind to be re-hydrated (the bug this fixed). History under **other** real session keys + `recentSessions` are preserved (other threads stay loadable from Recent; DB rows are untouched), and the next turn lazily creates a fresh session so the chat returns to the empty greeting. Contact capture is no longer a client concern — phone/email are captured server-side from the visitor's message by the watcher in `services/crm/session.ts` (the `ContactCard` + `CAPTURE_CONTACT` flow was removed). |
| `ChatHero` | `components/shells/membership/ChatHero.tsx` | `ChatHeader` renders once, full drawer width, above a row of `Sidebar` + the chat column + the memory panel — **not** nested inside the chat column, which is how it rendered before 2026-08-08 (PR #302; see the `ChatHeader` row below for why that changed). **When `isGated`** (from `useChatStore`): renders `<GateView />` occupying the full content area — no composer. **When not gated**: renders `MessageList` (once `hasStarted`), else the empty-state greeting — **personalized** ("Welcome, {invitedName}." heading + tagline subtitle) when `hasInviteToken && invitedName`; generic **"What's a story worth keeping?"** otherwise — followed by `ChatInput` + `SaveChatCTA`. **iOS keyboard handling**: `useKeyboardViewport({ active: state.isChatOpen, lockBodyScroll: true })` — surface shrinks to `vv.height`px when keyboard is open. **Memory panel (Stages A–E shipped 2026-08-08, PRs #302, #306–#308):** clicking a saved memory (`MemorySavedReceipt`, memory bookmark row below) sets local `openMemory` state here, rendering a third pane — `MemoryCardView` (own row below), the real chrome that replaced the Stage A throwaway stub (PR #312, 2026-08-08): a header with a live-editable title, eyebrow/date meta, a stubbed "add to story" button, and close; a scrollable body with a per-kind media placeholder and the read-only passage; and a persistent icon-only footer (Talk about this / Use as a base, both stubbed; Remove, wired to the real discard action, routed through the same delete-confirmation dialog session/story deletes already use) — and forcing `SidebarV2` to its collapsed rail via its new `forceCollapsed` prop (see `SidebarV2` row below). The panel's width is real, drag-resizable state, not a fixed split: `panelWidth` (px) drives the panel's `flexBasis` inline style (Tailwind can't express a runtime-computed value as a static class) and is resized through `MemoryPanelDivider` (own row below), mounted only while `openMemory` is set. `resetPanelWidth` — one `useCallback` wrapping `seedPanelWidth(panelRowRef.current.clientWidth)` (see the `memoryPanelWidth.ts` row below) — seeds `panelWidth` on the panel's own null→non-null `openMemory` transition and is reused as-is by the divider's Home/double-click reset (Stage E), so both paths reseed against the row's *current* width, never a frozen value from whenever the panel first opened. `isDraggingPanel` (set by the divider's `onDragStateChange`) suppresses the panel's own open/close CSS transition only while a drag is actually live, so a resize tracks the cursor instead of animating 300ms behind it. **Stage F (mobile) — shipped 2026-08-09:** `onOpenMemory` is now `handleOpenMemory` unconditionally (no more `isMobile ? undefined : ...` gate), so the saved-memory receipt is clickable on mobile too. Tapping it opens `MemoryCardView` (own row below) as a full-screen overlay — `absolute inset-0 z-40`, an inner `hl-animate-sheet` wrapper at `h-[100dvh]`, no rounding, no `border-t` — deliberately distinct from the Media pane's partial `85vh` sheet (immediately below) since there's no exposed background around a true `inset-0` overlay to round off. **No scrim**, also unlike Media's: a scrim earns its keep dimming the visible strip above Media's 85vh sheet and catching a dismiss-tap there, but a fully opaque `inset-0` overlay leaves nothing exposed to dim or tap outside of — dismissal is the panel's own existing close button only, same as desktop. **The mobile `SidebarV2` drawer is a third no-scrim case, as of 2026-08-12** (`sidebar_uploads_scrim_stories_2006` handover) — for a different reason than either: it isn't a modal at all, just a wider panel sitting on top of the chat undimmed, so dismissal relies on Escape, the header hamburger toggle, and the existing New Chat/select-conversation auto-close rather than a *dimming* tap-outside catcher (see the `SidebarV2` row below). **Tap-outside-to-close restored 2026-08-16 (mobile-sidebar-tap-outside):** that handover flagged, rather than resolved, the side effect of deleting the scrim — the scrim had also been the tap-outside target, so from 2026-08-12 the mobile drawer could only be dismissed by its own Close-X (Escape and the hamburger exist but are not reachable gestures for a touch user). The handover itself named the fix: “a different, invisible tap-catcher.” That is now what renders — a bare `absolute inset-0 z-20` div with an `onClick` dispatching `TOGGLE_SIDEBAR`, `aria-hidden` and roleless (it is the pointer affordance, not a control; keyboard dismissal stays the existing capture-phase Escape handler). Deliberately carries **no** `bg-*` and no `hl-animate-fade`: the no-dimming decision is untouched, only the lost dismiss path is restored. `z-20` keeps it under the drawer's own `z-30`, so the drawer stays fully interactive. `ChatHero.mobileSidebarScrim.test.tsx` covers both halves — tap-outside closes, Close-X still closes — and asserts the element carries no background class, so re-adding dimming fails there first. The broader scrim-everywhere vs scrim-nowhere standardization question that handover raised is still open and deliberately untouched by this change. Reuses `hl-animate-sheet` at its existing 0.24s duration rather than a slower one-off timing, keeping one motion system app-wide. Uses the same `z-40` tier Media uses (not a new one) since the two panes are mutually exclusive by construction and never need to out-rank each other. See `System Docs/Known Gaps.md`'s Memories entry for the full shipped-vs-outstanding breakdown. **Media pane (wired 2026-08-09):** a sibling `mediaOpen` boolean state (not a `MemoryRow`-shaped value like `openMemory` — `MediaGallery`, own row below, has no "which item" concept, it fetches its own full list) renders `MediaGallery` in the same third-pane wrapper the memory panel uses, on desktop, at a preferred `MEDIA_PANEL_WIDTH` (400px, no drag-resize) rather than `panelWidth` — as of 2026-08-14 (PR #379) this is clamped, not fixed: `mediaPanelWidth` state is seeded via `clampWidth(MEDIA_PANEL_WIDTH, MIN_PANEL_WIDTH, maxPanelWidth(total))` (reusing the memory panel's own `memoryPanelWidth.ts` math) on the pane's closed→open transition, so a narrow desktop viewport (collapsed 48px rail + 260px chat-column floor leaving less than 400px) shrinks the pane instead of overflowing the row and clipping `MediaGallery`'s close button, which is what happened before this fix; a `border-l border-border` on the wrapper substitutes for the seam `MemoryPanelDivider` normally provides, since the divider is memory-only and never mounts for media. `handleOpenMedia`/`handleOpenMemory` are wrapping handlers — not a shared enum — that each clear the other's state, so the two panes stay mutually exclusive by construction. Both `SidebarV2` call sites now pass `onMedia` (previously always `undefined`, see the `SidebarV2` row below) and (**as of 2026-08-14, PR #385** — was `{!!openMemory || mediaOpen || !!adminStoryId}`) `forceCollapsed={!!openMemory || mediaOpen || !!adminStoryId || sessionMemoriesOpen || !!storyViewId}` (desktop only — mobile never used `forceCollapsed`; `adminStoryId` added by `StoryAdminPanel`, see `System Docs/Known Gaps.md`'s story-admin-panel entry; `storyViewId` added by `StoryView` the same day as `sessionMemoriesOpen`, via a separate branch merged into this one — see the Session memories panel paragraph below; see the `StoryAdminPanel`/`StoryView` rows below for the panes themselves). **Mobile has no *shared* third-pane host** (unlike desktop's single resizable wrapper below) — Media and the mobile memory panel (Stage F, above) each get their own independent mobile overlay rather than sharing one. Media opens as a bottom sheet — a new `isMobile && mediaOpen` block reusing the exact `hl-animate-sheet` + `hl-animate-fade` scrim pattern already used by `SourceMenu.tsx`/`SaveChatCTA.tsx`, not a new animation. The mobile `SidebarV2`'s `onMedia` handler also dispatches `TOGGLE_SIDEBAR` to close the drawer first, mirroring the existing "New Chat"/session-row pattern inside `SidebarV2.tsx` of closing the mobile overlay after acting. **Stage 1 split, 2026-08-13 (`media_stages_08_2026`):** `mediaOpen`/`MediaGallery` is now the **in-chat** panel only, scoped to `state.sessionId` (`MediaGallery`'s new `sessionId` prop → `/api/media?chat_id=`), and is opened by a new `ChatHeader` icon (`onOpenMedia={handleOpenMedia}`, "Media from this chat" — see the `ChatHeader` row above; no such icon existed before this stage, so it was added rather than reusing `SidebarV2`'s "Media" row) instead of `SidebarV2`'s "Media" row. `SidebarV2`'s "Media" row now opens a new, separate `mediaPageOpen` boolean → `MediaPage` (own row below), a full-screen standalone page independent of any session, showing every account file — both `SidebarV2` call sites pass `onMedia={handleOpenMediaPage}` (desktop) / an inline handler wrapping it (mobile, still closing the drawer first). `handleOpenMediaPage` clears both `openMemory` and `mediaOpen`, and `handleOpenMemory` now also clears `mediaPageOpen` — needed because `MediaPage` and the mobile full-screen memory overlay (Stage F, above) both render at the same `absolute inset-0 z-40` layer (see `MediaPage.tsx`'s own comment on why it can't be `position: fixed` here, unlike its design-prototype source), so leaving one open underneath the other would stack two interactive full-screen dialogs rather than cleanly replacing one with the other. Card rendering (`MediaCard`, icons, `prettySize`) and the flex-wrap grid layout (`flex-wrap`, 12px gap, each card `flex: 1 1 220px` / min 180 / max 320 — 3 columns at the 780px container cap both surfaces share) were extracted out of `MediaGallery.tsx` into `components/shells/membership/media/MediaCard.tsx` and `media/MediaItemsGrid.tsx` so `MediaGallery` and `MediaPage` render identical cards; card anatomy itself is unchanged (Stage 2/3 scope). **Session memories panel (shipped 2026-08-14, PR #385)** — closes the gap `Design Handovers/ Aug 2026 Atomic Updates/11_session_memory_icon_gap/README.md` found: main had no "browse every memory kept this session, open one" flow, only the prototype did. A sibling `sessionMemoriesOpen` boolean (same shape as `mediaOpen` — no "which item" concept of its own; the new `SessionMemoriesPanel`, own row below, reads the shared `memories` list this file already owns) renders in the same third-pane wrapper as the memory/media/admin panes, opened by a new `ChatHeader` icon (`onOpenSessionMemories={handleOpenSessionMemories}`, "Memories from this chat" — see the `ChatHeader` row below) rather than the sidebar's `SidebarMemoryCount` badge, which stays a plain non-interactive `<span>` — a locked decision from the investigation, not an oversight. `handleOpenSessionMemories` is a wrapping handler in the same style as `handleOpenMedia`/`handleOpenMemory` — clears every sibling pane's state (including `storyViewId`, below) — and every other pane's own open-handler was updated to clear `sessionMemoriesOpen` in turn, so all five panes (`openMemory`/`mediaOpen`/`adminStoryId`/`sessionMemoriesOpen`/`storyViewId`) stay mutually exclusive by construction. Uses the same clamped `mediaPanelWidth` (no drag-resize) media/admin already use. **Mobile** renders it as an `85vh` bottom-sheet with scrim (`isMobile && sessionMemoriesOpen`), matching Media's own treatment — a browsable list, not a single-item editor — rather than the full-screen no-scrim treatment `openMemory`/`adminStory`/`storyViewStory` use; tapping a row calls `handleOpenMemory`, which closes this sheet and opens the mobile full-screen `MemoryCardView` overlay in its place, the same handoff the desktop pane has. **Real Story View lands the same day (`storyViewId`/`storyViewStory`, PR #384, `Design Handovers/ Aug 2026 Atomic Updates/01_real_story_view`)** — a separate branch adding its own new pane to the same shared group at the same time, merged into this branch mid-flight; every shared conditional (third-pane visibility/width/`border-l`, `forceCollapsed` above, the `mediaPanelWidth` recompute effect's dependency list) was updated by hand to list all five states, not four, rather than one side's flag silently winning the merge. **Real Story View, Phases 1b–1d (all 2026-08-14, same design handover — see the `StoryView`/`StoryMemoryEditor` rows below for the panes themselves):** row-tap-to-editor (real-story-view-1b-row-tap-editor) added a second piece of state, `storyMemory: { id, sessionId } | null` — which memory (if any) is swapped in for the list within the SAME third-pane slot; `storyViewId` itself stays set the whole time a memory is open, so the pane only ever fully closes via its own explicit close button. `handleOpenStoryView(storyId)` opens the list (clearing every sibling pane plus `storyMemory`); `handleOpenStoryMemory(memoryId, sessionId)` (passed to `StoryView`'s `onOpenMemory`) swaps in `StoryMemoryEditor`; `handleCloseStoryMemory()` clears just `storyMemory`, backing out to the list; `closeStoryPane()` clears both `storyViewId` and `storyMemory`, closing the whole pane — used by the pane's own close button and by every other pane's open-handler. Reordering (real-story-view-1c-reorder) needed no `ChatHero.tsx` state at all — `StoryView`'s own up/down move buttons PATCH directly and the parent pane doesn't care about order. **Real entry point (real-story-view-1d-entry-point) replaces the temporary `?storyView=<id>` query param** Phases 1a–1c used to verify the pane before this landed (that query-param mount effect is gone outright, not left alongside): both `SidebarV2` instances now receive `onSelectStory={handleSelectStory}` (mobile's inline wrapper also dispatches `TOGGLE_SIDEBAR` first, closing the drawer, matching the `onMedia` wrapper's existing pattern — `SidebarV2`'s story row doesn't call `onClose` internally the way its New Chat/session rows do). `handleSelectStory(storyId)` checks `GET /api/stories/[id]/memories` on click — there is still no `memoryCount`/`contentCount` field on `Story` anywhere in this codebase, see `System Docs/Known Gaps.md` for why that field was never built — and branches: non-empty calls `handleOpenStoryView`; empty closes every pane and calls the real `newChat()` then `setSessionContextToAttach({ contextType: 'story', contextRefId: storyId, contextFrequency: 'every_turn' })` (`newChat()` first is required — it clears the pending-context ref `setSessionContextToAttach` writes to), starting a fresh chat scoped to the story via the real session-context service (`services/chat/server/session-context.ts`) rather than the discarded `2026-08-13-story-click-routing` branch's one-time injected chat message. Any check failure (network, non-2xx) defaults to opening `StoryView` rather than silently starting an unrelated chat — its own already-built error state is the fallback, not a new one. `onStartStoryChat` (a separate, always-visible per-row icon distinct from the row's own click) remains unwired. **Stale-session-rows fix (found by automated PR review before merge):** `useMemories.ts` doesn't clear its `memories` array when `sessionId` changes (only `loadedForSessionId`/`isLoaded` resets), and nothing here closes `sessionMemoriesOpen` on a session switch — so `memories.memories` passed straight through would show a just-abandoned session's rows under "this session" after New Chat or a mid-fetch session switch. A `currentSessionMemories` derivation (`memories.memories.filter(m => m.session_id === state.sessionId)`) fixes it with no extra fetch; see `System Docs/Known Gaps.md`'s Memories entry for the full incident. |
| `MediaGallery` | `components/shells/membership/MediaGallery.tsx` | **In-chat** media panel — list of media items for the active chat session only (**2026-08-13, Stage 1 of `media_stages_08_2026`**: a new required `sessionId: string | null` prop drives `fetch(`/api/media?chat_id=${sessionId}`)`; a null `sessionId` — a brand-new, not-yet-saved chat — skips the fetch entirely and shows the empty state rather than falling back to every account item. Before this it fetched `/api/media` with no param at all, i.e. it was accidentally showing every account file, not session-scoped as intended — see the standalone `MediaPage` row below for the surface that's deliberately unscoped). Self-contained `flex flex-col h-full bg-background` with its own header ("Media" heading + close `X`, `aria-label="Close media gallery"`) and a scrollable body: the shared `media/MediaItemsGrid.tsx` flex-wrap grid of `media/MediaCard.tsx` cards (extracted out of this file in Stage 1 so `MediaPage` renders identical cards) — type icon, filename, status badge (Ready/Processing/Failed), size + date, expandable extracted-content text, and per-item actions — Download (`GET /api/media/{id}/url`, opens the signed URL in a new tab, ready items only) and a settled-state reprocess action hitting `POST /api/media/{id}/retry` (flips the item back to `pending` locally), labeled "Try again" on failed items or "Reprocess" on ready ones — same request, distinct copy since re-running an already-successful item reads as a different user intent than recovering a failure. **2026-08-10:** the route's own gate loosened from failed-only to any settled status (`'ready'` or `'failed'`, rejecting only `'pending'`/`'processing'`, which are already in-flight), specifically to let a `'ready'` item with genuinely wrong `derived_content` — e.g. from a since-fixed pipeline bug — be corrected without re-uploading (PR #327; see `Utilities/Media.md` and `Known Gaps.md`); an empty state when there are zero items. Built standalone; unmounted from navigation until **2026-08-09**, when `ChatHero.tsx` wired it in as the desktop third pane and a mobile bottom sheet (see the `ChatHero` row above) — opened via `SidebarV2`'s "Media" nav button at first, then (Stage 1, 2026-08-13) rewired to `ChatHeader`'s new "Media from this chat" icon once `SidebarV2`'s "Media" row was repointed at the standalone `MediaPage` instead (see the `SidebarV2` row below). **Loading-state skeleton (2026-08-13, PR #369, closing `Design Handovers/ Aug 2026 Atomic Updates/10_media_list_skeleton`):** the `loading` branch swaps a centered `Loader2` spinner for a 6-card skeleton grid (`MediaCardSkeleton`, defined locally in this file — no shared component exists for this exact loading-state region, so `MediaPage`'s identical block below is a duplicate, same convention as the spinner it replaced) previewing the real `MediaCard` shape (own row below): a `16/10` shimmer thumbnail block plus two shimmer text-line placeholders (70%/45% width) for filename + metadata. Reuses the shimmer already shipped for in-progress uploads — `animate-upload-shimmer` (`UploadThumbnail.tsx`, `tailwind.config.js`) — rather than the design handover's own `up-shimmer-bar` class, which was confirmed prototype-only (`chat-widget-canvas.jsx`) and never shipped to `main`. Wrapping grid carries `aria-busy="true" aria-label="Loading media"`. Covered by `MediaGallery.test.tsx`'s deferred-fetch test (holds the mock fetch open to actually observe the skeleton before it resolves). **Incremental pagination (2026-08-14):** the single `fetch(`/api/media?chat_id=${sessionId}`)` above is now the first of potentially several — driven by the new shared `media/useMediaPagination.ts` hook, which fetches 24-item pages and appends further pages (no re-triggering the skeleton above) as an `IntersectionObserver` sentinel at the grid's bottom scrolls into view, deduplicating by item id. See `Utilities/Media.md`'s "Media list pagination" section for the full mechanism and why it applies here despite one chat's media being naturally smaller than the whole account. |
| `MediaPage` | `components/shells/membership/MediaPage.tsx` | **Standalone top-level Media page (2026-08-13, Stage 1 of `media_stages_08_2026`)** — independent of any chat session, shows every account file (`fetch('/api/media')`, no `chat_id`). Opened by `SidebarV2`'s "Media" row (both desktop and mobile call sites, via `ChatHero.tsx`'s `handleOpenMediaPage`); distinct from `MediaGallery`'s in-chat, session-scoped panel (own row above). Always mounted (not conditionally rendered) so its `translateX(100%) → translateX(0)`, `.45s cubic-bezier(.22,1,.36,1)` slide-in can animate; `inert` while closed, matching `ChatDrawerV2`'s own closed-state contract. Renders `position: absolute; inset: 0` (not the design reference's `position: fixed`) at `z-40` against `ChatDrawerV2`'s own `relative` body — the design prototype (`Design Handovers/media_stages_08_2026/production-reference/chat-widget-canvas.jsx`) has no transformed ancestor to worry about, but real `ChatDrawerV2` carries `translate-x-*` (a `transform`) on its root, which establishes a new containing block for any `fixed` descendant; `absolute inset-0` against the drawer's relative body is the same fix already used by `ChatHero.tsx`'s mobile Media bottom sheet and mobile memory overlay (see the `ChatHero` row above). Header: a small feather-in-badge "Heirloom" brand mark (left, matching `MessageList.tsx`'s assistant-avatar icon asset), a stubbed "Upload" button (`onFlash('Upload is coming soon')` — no upload flow wired yet, Stage 3 scope) and Close (`aria-label="Close media page"`, distinct from `MediaGallery`'s "Close media gallery" so the two don't collide when both exist in the DOM at once). Body: centered column, `max-w-[780px]`, `px-6 py-7` (24px/28px, matching the design reference's `28px 24px`), rendering the shared `media/MediaItemsGrid.tsx` grid. Escape closes it (window keydown listener, active only while open); a `bg-black/45` backdrop layer (same `z-40`, `aria-hidden`, fades with the panel) also closes it on click — not mentioned in earlier passes of this row. Opening it also clears `openMemory` and `mediaOpen` (`handleOpenMediaPage` in `ChatHero.tsx`), and opening a memory or the in-chat panel clears it back — required because it shares the same `absolute inset-0 z-40` layer as the mobile full-screen memory overlay; without the mutual clearing, two full-screen overlays could end up stacked with no way to tell which one has focus. **Loading-state skeleton (2026-08-13, PR #369):** same `MediaCardSkeleton` swap as `MediaGallery`'s identical loading branch (own row above, full description there) — duplicated locally here since no shared component covers this loading-state region. Covered by `MediaPage.test.tsx` (new file, same deferred-fetch technique as `MediaGallery.test.tsx`'s). **Incremental pagination (2026-08-14):** `fetch('/api/media')` above is now page one of a scroll-driven sequence via the shared `media/useMediaPagination.ts` hook (own mechanism described in `MediaGallery`'s row above) — the account-wide list was the more severely affected of the two consumers before this, since it had no natural per-chat bound at all. Fetches once per `MediaPage` lifetime (the first time it's ever opened), not on every reopen — see the component's own `hasOpened` comment. |
| `MediaCard` / `MediaItemsGrid` | `components/shells/membership/media/MediaCard.tsx`, `media/MediaItemsGrid.tsx` | **Card anatomy — 2026-08-13, Stage 2 of `media_stages_08_2026`** (continuing Stage 1's grid extraction, own rows above). Vertical, image-forward card: a `16/10` thumbnail area on top, `StatusBadge` (Ready/Processing/Failed — unchanged states, just moved here from its old spot inline with classification/size/date) absolutely positioned top-left of it, then the pre-existing body/footer below (filename, classification + size/date, expandable extracted content, failed-state error line, Download/Reprocess — untouched, Stage 3 scope). Thumbnail resolves a real photo for `type: 'image'` items with a display url (`object-fit: cover`) — investigated and confirmed real at Stage 2: `GET /api/media` already attaches a batched signed url per image item (`withDisplayUrl`, `services/media/display-url.ts`) and `useFreshImageUrl` (own row in `Utilities/Media.md`) re-resolves it past its 60s expiry, the same mechanism `MessageList.tsx`'s chat images and `BlockCanvas.tsx`'s memory-panel image blocks already use — `MediaGallery`/`MediaPage`'s fetched-items type widened from `MediaItem` to `MediaItemWithUrl` accordingly. No `'video'` member exists on `MediaItemType` today, so no video-thumbnail/play-badge code was built — `MediaTypeIcon`'s three real branches (audio/image/document) are unchanged, just now sized 24px for the fallback tile instead of the old 16px icon chip. Fallback (audio, document, or an image with no display url yet): a per-type tinted tile — `MEDIA_TILE_TINT`, `bg-accent/[opacity]` (8%/5%/3% image/audio/document) layered over an opaque `bg-surface-2` base, not CSS `color-mix()` (avoids a browser-support question for one cosmetic tint) — with a centered `56px` `bg-accent-soft`/`border-accent/35` circular badge holding the type icon, same visual language as `MediaPage`'s own header brand-mark badge. Covered by `media/MediaCard.test.tsx` (thumbnail vs. fallback tile, status badge in both states, fresh-url re-resolution). **Stage 3 additions, 2026-08-13 (`media_stages_08_2026/stage-3-icon-actions-workflows`, folding in the Aug 2026 Atomic Updates `09_media_metadata_lazyload` handover):** footer gains three new icon-only buttons (`CardIconButton`, styled after `MemoryCardView`'s own icon-only footer actions) — `+` add-to-memory (opens `media/AddToMemoryPanel.tsx`, own row below), pen edit-stub (`onEditStub` → "Editing media is coming soon" toast), and a right-aligned (`ml-auto`) trash delete (opens the now-generalized `ConfirmDeleteModal`, own row below) — added **alongside** the existing Download/Reprocess, not replacing them (the design prototype removes Download; this codebase's Reprocess is a real feature the prototype doesn't have at all, so neither was touched). Thumbnail `<img>` now carries `loading="lazy"` (no fade-in wrapper — one was tried and reverted as unreliable per the handover). The metadata line now always states the upload date, appending "· Processing" instead of the file size while `status === 'processing'` — additive to the separate status badge, not a replacement. Filename is click-to-rename (pencil affordance, inline `<input>`, autofocus, commit on Enter/blur, Escape cancels without forcing a blur — mirrors `MemoryCardView.tsx`'s own fix for the same stale-value-on-synchronous-blur bug) via a new `onRename(id, name)` prop — **local-state only, no PATCH endpoint exists for media items; see `Known Gaps.md`.** |
| `AddToMemoryPanel` / `useMediaItemActions` | `components/shells/membership/media/AddToMemoryPanel.tsx`, `media/useMediaItemActions.ts` | **Stage 3 of `media_stages_08_2026`, 2026-08-13.** `AddToMemoryPanel` is the "+" icon's slide-in panel — `SessionMemoriesPanel` (which the handover assumed existed, "reuse it in select mode") doesn't exist anywhere in real code, so this is the minimal select-multiple list built instead, styled to match existing list conventions (`memoryKinds.ts`'s `KIND_ICONS`/`memoryKindOf` for the per-row icon). Lists the **media item's own originating chat's** real saved memories (`useMemories(item.chat_id)`, not a `memories` prop threaded down from `ChatHero` — no cross-session memory concept exists to plumb otherwise) as checkbox rows (`role="checkbox"`); confirming shows a toast (`flash`) and closes after a 600ms delay — **no write**, matching the design's own spec exactly (investigated whether a real attach-to-existing-memory mechanism exists to reuse instead of stubbing it — it doesn't; see `Known Gaps.md`). Slides in from the right, `absolute inset-y-0 right-0`, `width: min(400px,100vw)` (not `position: fixed` — same drawer-transform reasoning as `MediaPage`'s own doc comment), reuses `useModalA11y` (Escape/focus-trap/focus-restore) like the other V2 modals. `useMediaItemActions.ts` exports `useMediaDelete(setItems)`, the small shared `pendingDelete` state + `confirmDelete` (filters the item out of local state — no `DELETE` endpoint exists, see `Known Gaps.md`) both `MediaGallery` and `MediaPage` use identically to drive their own `ConfirmDeleteModal` instance. `ConfirmDeleteModal` (`v2/ConfirmDeleteModal.tsx`, previously chat/story/memory-only) gained optional `heading`/`body`/`confirmLabel` override props this stage — it was not already generalized as the handover assumed — plus a local `'media'` addition to its own target union (not the shared `v2/types.ts RowTarget`, so `SidebarV2`'s kebab switch is untouched). Media's body copy deliberately avoids the default "permanently removed, can't be undone" wording, which would be false for a non-persisted removal. Covered by `media/AddToMemoryPanel.test.tsx` and `media/MediaCard.test.tsx`'s footer-actions describe block. |
| `MemoryPanelDivider` | `components/shells/membership/MemoryPanelDivider.tsx` | The draggable, keyboard-operable boundary between the chat column and the memory panel — `ChatHero.tsx` mounts it only while `openMemory` is set (nothing to drag with the panel closed). Built across three stages, each independently reviewable: **Stage C** (PR #306) drag interaction — `onPointerDown` reads the panel's current width via `onStart()`, then a window-level `pointermove`/`pointerup` pair (not React's synthetic events, so tracking survives the cursor leaving the 9px hit-zone mid-drag) calls `onMove(base, delta)` on every move and tears down on `pointerup`; `document.body.style.cursor`/`userSelect` are set for the drag's duration so text doesn't select while resizing. **Stage D** (PR #307) hover/drag visual treatment — two boolean states, `hot` (hovered *or* focused) and `live` (actively dragging), combined into one `on` flag that drives every visual: an accent-colored seam line (`bg-border` at rest), a center grip pill (`opacity-0` at rest), and a `bg-accent/[0.12]` background wash on the hit-zone itself. `hot`/`live` are tracked separately but rendered identically on purpose — combining them means the treatment doesn't flicker off if a fast drag carries the cursor outside the 9px zone. **Stage E** (PR #308) keyboard operability — `tabIndex={0}` makes the divider tab-reachable; `onFocus`/`onBlur` feed the *same* `hot` state hover already uses, so keyboard focus gets the identical visual treatment, not a separate focus style; `ArrowLeft`/`ArrowRight` call the exact same `onMove(onStart(), ±16)` a pointer drag calls (a fixed 16px step instead of a live pointer delta — no second width-calculation path); `Home` and double-click both call one `onReset` prop, not two implementations of "reset." **Props:** `onStart(): number` (panel width at drag/nudge start), `onMove(base, delta)` (pure math on the caller's side — no DOM reads in this component, which is what makes it unit-testable without a real layout engine), `onReset?: () => void` (optional — Home/double-click are safe no-ops without it), `onDragStateChange(dragging: boolean)` (fires true/false at drag start/end so `ChatHero.tsx` can suppress its own open/close transition only while a drag is live), `label: string` (the `aria-label`). `role="separator"` + `aria-orientation="vertical"`. Adapted from (not copied from) `Curtain.tsx` in `Design Handovers/design_handoff_memory_panel_layout_2026/`, which bundles all three concerns into one component — pulled apart across stages here so each stayed reviewable on its own. Unit-tested in `MemoryPanelDivider.test.tsx` (hover/drag treatment + all three keyboard behaviors, in isolation); the drag mechanic's integration with `ChatHero.tsx`'s real state is covered separately in `ChatHero.memoryPanel.test.tsx`. |
| `memoryPanelWidth.ts` (pure width math) | `components/shells/membership/memoryPanelWidth.ts` | No React, no DOM reads — every function takes `total` (the panel row's `clientWidth`, read by the caller) and returns a number, which is what makes this trivially unit-testable and keeps `ChatHero.tsx`'s own drag math free of duplicated logic. `clampWidth(v, lo, hi)` clamps `v` between `lo` and `hi` with **`hi` winning when they conflict** (`lo > hi`) — `Math.min(hi, Math.max(lo, Math.round(v)))`, deliberately nested with `Math.min` as the outer call. The naive `Math.max(lo, Math.min(hi, v))` ordering always returns `lo` when `lo > hi` regardless of how little room `hi` actually represents — the exact priority bug found and fixed (same session) in the reference `Curtain.tsx`'s own version of this function. `maxPanelWidth(total)` is the most the panel can ever claim: `total` minus the collapsed sidebar rail (`RAIL_WIDTH = 48`, matching `SidebarV2`'s own `w-12` while `forceCollapsed`), the divider's own width (`DIVIDER_WIDTH = 9`), and the chat column's floor (`MIN_CHAT_WIDTH = 260`) — floored at `MIN_PANEL_WIDTH` (`280`) so callers never see an inverted range. **`MIN_CHAT_WIDTH`/`MIN_PANEL_WIDTH` must stay in sync with the literal Tailwind classes in `ChatHero.tsx`** (`min-w-[260px]` on the chat column, `min-w-[280px]` on the panel) — Tailwind can't scan a JS-interpolated arbitrary value, so those two class strings are hand-kept equal to these two constants rather than generated from them; verified safe at the drawer's own narrowest width (`ChatDrawerV2`'s `clamp(680px,50vw,1120px)` floor): `48 + 9 + 260 + 280 = 597`, leaving 83px of slack. `seedPanelWidth(total)` is the panel's default width on open (and, as of Stage E, on a Home/double-click reset) — `clampWidth(Math.round(total * DEFAULT_PANEL_FRACTION), MIN_PANEL_WIDTH, maxPanelWidth(total))`, `DEFAULT_PANEL_FRACTION = 0.55` — always recomputed against the *current* `total`, never a frozen constant, so a reset after a window resize reflects the new size rather than whatever it was when the panel first opened. |
| `MemoryCardView` | `components/shells/membership/memory/MemoryCardView.tsx` | The real chrome for one open memory in the memory panel (PR #312, 2026-08-08), replacing `ChatHero.tsx`'s throwaway Stage A `MemoryPanelStub`. Adapted from (not copied from) `MemoryCardView.tsx` in `Design Handovers/design_handoff_memory_canvas_08_2026/` — chrome shape only; several of the reference's actions have no backend support in this codebase and are deliberately stubbed rather than faked (see the file's own doc comment for the full mapping). **Header:** title is live-editable — the PATCH route's existing `retitle` action (`renameMemory()`, already backing `MemorySavedReceipt`/`MemoryCard`'s own inline rename) reused here, committed on blur/Enter only, same pattern as `MemoryCard.tsx`'s draft-card title edit — not the reference's per-keystroke `onChange`, which would spam a PATCH on every character. Local `titleDraft` state resets whenever the OPEN memory itself changes (id/title change), so a half-typed draft on one memory never bleeds into the next if the visitor clicks straight from one saved receipt to another without closing the panel first. Escape reverts the draft WITHOUT forcing a blur (a real bug found and fixed the same day: blurring synchronously right after `setTitleDraft` reads the STALE pre-revert value, since the state update is async — committing the very text Escape was meant to discard). Eyebrow/date meta is read-only. The "+" (add to a story) button is real (as of 2026-08-13, PR #377) — it renders `StoryPicker` (`components/shells/membership/memory/StoryPicker.tsx`), a popover matching `BlockInserter`'s own dismissal mechanics (backdrop + Escape) that lists every story the signed-in member owns or is subscribed to via a `stories: Story[]` prop, sourced from `ChatHero.tsx`'s own `stories` state (`GET /api/stories`) — the same list `InviteCollaboratorsModal`'s story picker already uses. A single click assigns the open memory to that story immediately via `onAssignStory` → PATCH `.../memories/[memoryId]` action `'assign_story'` → `assignMemoryToStory` (`services/crm/story-containments.ts`), no separate confirm step. The current story, if any, is highlighted with a checkmark; re-picking it is a no-op — `onPick` is never called, so no redundant PATCH fires (`StoryPicker.tsx`'s own `handlePick`). **The trigger button itself now reflects assignment state too (remove-memory-from-story, 2026-08-14):** a green filled `Check` circle (`bg-[#2E7D4F]`) when `currentStoryId` resolves against `stories`, the ordinary accent `Plus` circle otherwise — same 30px circle, visual-state only, still opens the same popover. When a story is currently assigned, the popover also renders a "Remove from '[Story]'" item at its top — same no-confirm posture as assigning — wired to a new `onRemoveFromStory` prop → PATCH action `'remove_story'` → `removeMemoryFromStory` (same file). `MemoryCardView`'s `onRemoveFromStory` is threaded from both `ChatHero.tsx` render sites (mobile + desktop, see this row's own memory-panel paragraph) and `StoryMemoryEditor.tsx` (Real Story View's own cross-session-safe editor, `v2/StoryMemoryEditor.tsx`). **`MemoryCard.tsx`'s `MemorySavedReceipt` is a fourth real caller, corrected on rebase (remove-memory-from-story, 2026-08-14):** an earlier pass here assumed its "+" was still a stub, which was true when that pass was written but stopped being true once PR #391 (memory-receipt-story-picker, 2026-08-14, merged to `main` independently, same day) wired the receipt's own "+"/checkmark trigger to a real `StoryPicker` — see the `MemorySavedReceipt` row below for that wiring. Rebasing this branch onto `main` after PR #391 surfaced the real consequence: `StoryPicker`'s `onRemove` prop is required, so the receipt's existing `StoryPicker` call needed `onRemoveFromStory` threaded the rest of the way too, mirroring exactly how PR #391 already threaded `onAssignStory` — `MessageList.tsx`'s `MessageListProps`/`MemorySlotHandlers` gained the matching optional `onRemoveFromStory?: (memory: MemoryRow) => void`, and `MemorySavedReceiptProps` gained `onRemoveFromStory?: () => void` (defaulted to a no-op, not left `undefined`, since `StoryPicker`'s own `onRemove` is required unlike `onAssignStory`). `ChatHero.tsx` passes the same `handleRemoveMemoryFromStory` it already uses for `MemoryCardView`, shared as-is. The trigger's checkmark/Plus visual state needed no separate wiring — it lives inside `StoryPicker.tsx` itself, so it applies automatically to every caller including this one. **Body:** scrolls independently between the fixed header and footer; a per-kind media placeholder box (same convention as `MemoryCard.tsx`'s draft card — no real thumbnail/waveform exists anywhere in this codebase yet, see `memoryKinds.ts`) renders above the passage when `kind.media` is set, omitted entirely (no empty box) otherwise. **The passage is editable via a block canvas (Memory Canvas V1, added 2026-08-08, revised same day per the Text+Image Scope Handover — text + image blocks only)**, backed by `reviseMemoryBlocks`/`revise_blocks` (`services/crm/memories.ts`). **The canvas renders immediately on open — no pencil, no separate "Edit mode," ever:** a memory with `body_blocks: null` gets a default single text block derived from `memory.body` (`buildDefaultBlocks()`, matching the reference prototype's own function of the same name), and `body_blocks`-populated memories render those blocks directly, in order. Local `blocksDraft` state resets from the canonical `memory.body_blocks` only when `memory.id` changes (switching to a different open memory) — deliberately NOT on every `memory.body_blocks` change, since with per-keystroke commits a slower-resolving earlier PATCH response could otherwise clobber a faster, not-yet-confirmed later keystroke (the reference has the same shape — its own reset effect is keyed on `[mem.id, mem.version]`, not on content changing). **Text-block content commits on every keystroke** (not blur-gated — matches the reference's own `patchBlock`/`commit()` exactly, per the handover's explicit confirmation), a real, accepted cost: every keystroke round-trips through `reviseMemoryBlocks`'s full validation and a DB write (the media-item ownership check in particular) — a short debounce was flagged as a possible future optimization, not implemented. Structural changes (insert text/photo at a position, remove a block) also commit immediately, matching `onKeep`/`onDiscard`'s own posture. Insert control: a `BlockInserter` "+" (`BlockCanvas.tsx`, own row below) sits before the first block and after every block — N blocks → N+1 slots, independent of reordering (out of scope) — expanding to exactly 2 icon options (text, image); picking "image" opens a picker of the session's own ready photos rather than inserting an unattached block, since an image block always needs a real `media_item_id`. A block can't be removed if doing so would leave zero non-empty text (disabled, not silently rejected) — the one client-side guard that survives from the first same-day attempt at this; the earlier attempt's blur-based "revert an emptied block" guard does not, since there's no more blur-gating to revert at. (The transcript's own `MemoryCard`/`MemorySavedReceipt`, own row below, are entirely unaffected — still read-only exactly as before; this panel remains the only surface where a memory's content is ever editable.) **Footer:** persistent, icon-only (not icon+label — fixed 2026-08-08, PR #312's second commit). The original label+icon version needed ~401px for three buttons, but the panel's default seeded width is only ~359px on any browser window under ~1360px wide (`ChatDrawerV2`'s own `clamp(680px,50vw,1120px)` floor — confirmed via a real Playwright/Chromium repro, not assumed, since jsdom/happy-dom have no real layout engine to catch this) — most laptops, not an edge case. Icon-only fits comfortably down to `MIN_PANEL_WIDTH` (280px, `memoryPanelWidth.ts`) with room to spare. "Talk about this" / "Use as a base" fire the same stub toast as "+"; **Remove** is the one footer action with real backend support (`discardMemory` via the shared `memories.discard`) — PR #312's third commit routed it through the same `pendingDelete` state + `ConfirmDeleteModal` session/story deletes already use (`RowTarget` gained a `'memory'` variant, `v2/types.ts`) rather than firing the discard directly with no confirmation, which is what it did on first ship. `ConfirmDeleteModal`'s copy got a real fix in the same pass — it defaulted to "chat" for anything that wasn't a `'story'`, so a memory delete would have said "Delete this chat?" and warned about "every memory it holds," both wrong for deleting a single memory. Unit-tested in `MemoryCardView.test.tsx` (title commit/revert/Escape behavior, media block presence/absence, footer wiring, deriving default blocks on open, and exactly what each kind of block edit sends to `onReviseBlocks`, in isolation — `BlockCanvas.tsx`'s own rendering/interaction details have their own test file); the Remove → confirm → discard flow through the real `ChatHero` stack is covered separately in `ChatHero.memoryPanel.test.tsx`. **Mobile (Stage F, 2026-08-09):** this component's `flex h-full flex-col` header/scrollable-body/footer layout needed no changes to work full-screen — already width/height-agnostic, proven by the desktop panel resizing it from 280px up; `ChatHero.tsx`'s new mobile block just mounts the exact same component in a bigger box. One real fix landed the same day, found on live mobile testing: the root `<div>` had no background color class at all, invisible on desktop (an in-flow flex sibling with nothing stacked beneath it) but letting the chat transcript bleed straight through the mobile full-screen overlay, which sits `position: absolute` directly on top of it. Fixed at the root with `bg-background` — matching `MediaGallery.tsx`'s identical third-pane root pattern — rather than patching `ChatHero.tsx`'s wrapper divs, so the component is opaque by construction in any context it's mounted in. Covered for mobile in `ChatHero.mobileMemoryPanel.test.tsx`. |
| `SessionMemoriesPanel` | `components/shells/membership/memory/SessionMemoriesPanel.tsx` | New 2026-08-14 (PR #385) — the third-pane list of every memory kept during the active session, opened via `ChatHeader`'s new "Memories from this chat" icon (see that row and the `ChatHero` row's Session memories panel paragraph for the entry point/mutual-exclusion wiring). Adapted from (not copied from) the prototype's own `SessionMemoriesPanel` in `chat-widget-canvas.jsx` — same anatomy (header with title + count, a scrollable list of rows: kind icon, title, date, "Featured in X" story tag when `memory.storyId` is set, a truncated passage preview, chevron) rebuilt against this codebase's real Tailwind chrome (the shared header shape `StoryAdminPanel.tsx`/`MediaGallery.tsx` already use) instead of the reference's inline styles — not the reference's select-mode variant, which is the photo-attach flow and out of scope here. Takes `memories: MemoryRow[]` **unfiltered by status** — draft and published both render, a draft row gets a small "Draft" label next to its title, and the subtitle reads "N memories this session" rather than the reference's "N kept this session" (deliberately status-neutral copy — "kept" would overclaim for a row that's still a draft). Each row is a single `<button>` calling `onOpen(memory)`, which `ChatHero.tsx` wires to the same `handleOpenMemory` the transcript's own memory receipts use — opens the clicked row into `MemoryCardView`, not a separate read-only view. Purely presentational — no fetch of its own; the caller (`ChatHero.tsx`) is responsible for scoping `memories` to the active session (`currentSessionMemories`, see that row's Session memories panel paragraph and `System Docs/Known Gaps.md`'s Memories entry for the bug this guards against). Covered by `ChatHero.sessionMemoriesPanel.test.tsx` (open/close, draft+published both listed, row click → `MemoryCardView`, mutual exclusion with Media/a transcript memory/Admin/Story View, sidebar force-collapse, mobile bottom sheet + row-tap handoff to the mobile memory overlay, and the stale-session-rows regression). |
| `StoryAdminPanel` | `components/shells/membership/v2/StoryAdminPanel.tsx` | New story-admin-panel (2026-08-13, `Design Handovers/ Aug 2026 Atomic Updates/Updated Story Kebabs/`) — the story kebab's "Admin" item (see `System Docs/Known Gaps.md`'s `RowMenu`/`story-admin-menu-item` entry for the target-aware menu wiring that added it). Mounts as a third pane in `ChatHero.tsx`, same self-contained-fetch pattern as `MediaGallery`/`StoryView` — no parent-held roster. Two pieces: **Description** — the story's `body` column, editable here for the first time since creation, via a `<textarea>` that commits on blur (`onDescriptionCommit`) only when the trimmed value actually changed; the parent (`ChatHero.tsx`'s `handleUpdateStoryDescription`) owns the real `PATCH /api/stories/[id]` call and updates its own `stories` state on success, since the description is also shown elsewhere (`SidebarV2`'s row tooltip) — same shape as `MemoryCardView`'s `onRetitle`. Local `desc` state resets only on a `story.id` change, not every re-render. **Members** — self-fetches its own roster (`GET /api/heirloom/story-invites?story_id=`, the same route the invite modal uses) showing each collaborator's initials avatar, name, and joined date; Remove opens a real `alertdialog`-role confirm (own inline markup, not the shared `ConfirmDeleteModal` — this removes a collaborator, not an artifact) before calling `DELETE /api/heirloom/story-invites/collaborators`; success drops the row from local state directly (no re-fetch), failure keeps the dialog open with an inline error rather than silently dropping the row or closing without explanation. Deliberately **no memory count per row** even though the design-handover mockup shows one — fabricating a number nothing tracked was rejected in the collaborator-removal pass that predates this component, and `listStoryCollaborators` still doesn't do the join that would compute a real one (see `System Docs/Known Gaps.md`'s "roster gap" paragraph). |
| `StoryView` | `components/shells/membership/v2/StoryView.tsx` | New real-story-view-1a-static-list (2026-08-14, `Design Handovers/ Aug 2026 Atomic Updates/01_real_story_view`) — the first real "open a story, see its memories" surface (previously a story's kebab only ever opened `StoryAdminPanel`, own row below, which deliberately shows no memory list). Mounts as a third pane in `ChatHero.tsx`, same self-contained-fetch pattern as `MediaGallery`/`SessionMemoriesPanel` above — no parent-held memory list, its own `GET /api/stories/[id]/memories` (`System Docs/API Routes.md`) on mount, keyed off `story.id`. Header: `story.name`, a real memory count (`N memories`, computed from the fetch, not hardcoded), and `story.isOwner ? ' · you own this story' : ''` — real (`GET`/`POST /api/stories`'s own `isOwner` field), only rendered when true, never fake/misleading text for a collaborator who reached the story via `artifact_subscribers`. Three states: a centered spinner while loading, "Could not load this story's memories" on fetch failure, an italic "No memories in this story yet" empty state, or the ordered `<ol>` of rows. **Row tap (Phase 1b, real-story-view-1b-row-tap-editor):** each row's title/body-snippet/date block is a real `<button>` (not a decorative `div` — semantic + keyboard-accessible per `CLAUDE.md`'s Accessibility principle) calling `onOpenMemory(memory.id, memory.session_id)` — both the id AND the memory's own `session_id` are needed, since the caller has to open a correctly cross-session-scoped editor (`StoryMemoryEditor`, own row below). **Reorder (Phase 1c, real-story-view-1c-reorder):** sibling `ChevronUp`/`ChevronDown` "Move up"/"Move down" icon buttons per row (not drag-and-drop — a deliberate simplification for this pass), disabled at the top/bottom edge of the list and, on every row at once, while any move is in flight (a second click on a different row before the first move's round trip + refetch resolves could otherwise race against a server order the client hasn't seen yet). `handleMove` PATCHes `/api/stories/[id]/memories` (`{ memoryId, direction }`) then refetches the whole list rather than reordering local state client-side — the server is the only place that can safely compute the new order (see `moveMemoryInStory`'s own doc comment, `services/crm/story-containments.ts`); a failed PATCH or refetch calls the shared `onFlash` toast ("Could not move memory") rather than silently no-opping. Deliberately out of scope for this whole view, per the design handover's own Phase 1 sequence: no "Share this story" / "+" add-existing-memories buttons (neither has a real production counterpart anywhere yet — only the design-handover mockup has them) and no "Publish this story" button (no defined behavior exists anywhere for it, not guessed at here). Opened via `ChatHero.tsx`'s `handleOpenStoryView`/`handleSelectStory` — see the `ChatHero` row above's Real Story View paragraphs for the full entry-point and mutual-exclusion wiring. Covered by `StoryView.test.tsx` (list rendering, loading/error/empty states, row tap, move buttons' edge-disabling and in-flight-disabling, move failure toast) and exercised through the real `ChatHero` stack by `ChatHero.selectStory.test.tsx`/`ChatHero.storyMemoryEditor.test.tsx` (own row below). |
| `StoryMemoryEditor` | `components/shells/membership/v2/StoryMemoryEditor.tsx` | New real-story-view-1b-row-tap-editor (2026-08-14) — opens the ONE memory tapped from `StoryView`'s row list, reusing `MemoryCardView` (own row above) as the actual editor chrome, but with its OWN `useMemories(sessionId)` instance scoped to the TAPPED MEMORY'S OWN session — deliberately not `ChatHero`'s `useMemories(state.sessionId)` instance, since a story's memories routinely come from a different chat session than whichever one is currently open (the whole point of a story is pulling memories together across conversations). This matters beyond data shape: `PATCH /api/sessions/[id]/memories/[memoryId]`'s four everyday actions (keep/discard/retitle/revise_blocks) are session-scoped for real, with no sign-in requirement — the URL's session id against the memory's own `session_id` column is their ONLY access control (`assign_story` is the one exception, real `getCurrentUserId()`-gated) — so reusing `ChatHero`'s own chat-scoped hook here would silently 404 every action except "+" for any memory from a different session, which is the common case, not an edge case. Same reasoning for the block canvas's image-attach picker: `sessionImages` is fetched directly here via `GET /api/media?chat_id=<this memory's own session>` (unpaginated — this only feeds a picker dropdown, not an account-scale gallery), rather than reusing `ChatHero`'s own currently-open-chat-scoped `sessionImages`, which would silently fail to show this memory's own linked photo or offer the wrong session's photos to attach. Renders a "not found" state (with just a close button) if the tapped memory doesn't resolve in the freshly-scoped hook's list. Remove routes through the same `ConfirmDeleteModal` session/story deletes already use. `onAssignStory` (via `MemoryCardView`'s `StoryPicker`) flashes "Added to X"/"Moved to X" depending on whether the memory already had a story. Purely a `ChatHero.tsx`-level wiring layer on top of two already-tested components — its own cross-session-safety is covered by `StoryMemoryEditor.test.tsx`; the pane-swap wiring (row tap → editor, close → back to list, whole-pane close) is covered separately by `ChatHero.storyMemoryEditor.test.tsx`, since neither `StoryView.test.tsx` nor this component's own test file in isolation can exercise the parent's slot-swap. |
| `StoryPicker` | `components/shells/membership/memory/StoryPicker.tsx` | The "+" (add to a story) popover — real as of assign-memory-to-story (2026-08-13), replacing the old "Coming soon" stub. Shared by two callers: `MemoryCardView`'s header "+" (own row above, PR #377) and, as of 2026-08-14 (memory-receipt-story-picker), `MemorySavedReceipt`'s in-transcript "+" (Memory bookmark row below) — same component, same mechanics, both PATCHing `.../memories/[memoryId]` action `'assign_story'` via each caller's own `onAssignStory`. Mechanics copied from `BlockCanvas.tsx`'s own `BlockInserter` exactly: a fixed, invisible, full-viewport backdrop (lower z-index than the popover) dismisses on any outside click, an `Escape` keydown listener does the same while keyboard-driven, and `close()` resets both. Props: `stories: Story[]`, `currentStoryId?: string \| null` (highlighted with a checkmark), `onPick: (storyId: string) => void`. Single click on a story assigns immediately and closes — no separate confirm step; this is **not** the checklist-with-footer-button pattern a design handover shows for a different, later, not-yet-built feature (bulk-adding several memories into a story from the story's own view) — a different entry point entirely. Re-picking the story the memory is already in is a deliberate no-op — `onPick` is never called, so the caller never issues a redundant PATCH (`handlePick`'s own guard). **Trigger reflects assignment state + supports removal (remove-memory-from-story, 2026-08-14):** a green filled `Check` circle (`bg-[#2E7D4F]`) when `currentStoryId` resolves against `stories`, the ordinary accent `Plus` circle otherwise — same 30px circle, visual-state only, still opens the same popover; when a story is currently assigned, the popover also renders a "Remove from '[Story]'" item at its top (no-confirm, same posture as assigning), wired to a required `onRemove: () => void` prop → PATCH action `'remove_story'` → `removeMemoryFromStory` (same file, `services/crm/story-containments.ts`). No Heirloom-scoped "positive"/"success" design token exists (checked `System Docs/Design System.md` — SBL's `--color-pos` is SBL-route-scoped only, inert here), so the checkmark state uses the literal reference hex directly, matching the arbitrary-hex precedent already in this codebase (e.g. `BookingCard.tsx`'s `bg-[#2d6a4f]`). Covered by `StoryPicker.test.tsx`; each caller's own integration is covered separately (`ChatHero.assignMemoryToStory.test.tsx`/`ChatHero.receiptAssignStory.test.tsx` for assignment, `ChatHero.removeMemoryFromStory.test.tsx`/`ChatHero.receiptRemoveFromStory.test.tsx` for removal). |
| `BlockCanvas` | `components/shells/membership/memory/BlockCanvas.tsx` | The block-canvas body for a memory (Memory Canvas V1, 2026-08-08; insert control and save behavior revised same day per the Text+Image Scope Handover, `Design Handovers/handover_memory edit panel_08_2026/`) — fully controlled/presentational, all state (the blocks array, when to persist) lives in `MemoryCardView.tsx`; this component only renders `blocks` and reports intent via callbacks (`onContentChange` per keystroke — no separate blur/commit callback anymore, `onAddText`/`onAddImage` per position, `onRemove`, `canRemove` to disable the one removal that would empty the passage). Text blocks are auto-resizing `<textarea>`s (manual `scrollHeight` resize on content change, same technique as the design reference's prototype — `field-sizing: content` isn't broadly supported yet). Image blocks look up their `media_item_id` in `sessionImages` (passed down from `ChatHero.tsx`'s own `useChatStore().mediaItems`, filtered to `type: 'image'` + `status: 'ready'` + a resolved signed `url` — no separate fetch); a media item with no match (stale reference, or not yet loaded) degrades to a "Photo unavailable" placeholder rather than a broken `<img>`. **Insert control:** a `BlockInserter` "+" sits before the first block and after every block (N blocks → N+1 slots — a 2-block memory gets top/between/bottom), independent of reordering, which stays out of scope. Clicking "+" reveals exactly 2 icon options (text, image), not the reference's 6-type picker (`BLOCK_KINDS` trimmed down for this scope); picking "image" expands into a picker of the session's own ready photos rather than inserting an unattached block — empty state reads "No photos in this conversation yet." rather than an empty grid. Deliberately absent, per the locked V1 scope: no drag-to-reorder, no video/quote/divider/gallery block types. No upload UI anywhere here — `media_item_id` only ever references an existing upload. **`BlockInserter` "+" restyled 2026-08-09** to match `MemoryCardView`'s header "add to story" button: `h-[30px] w-[30px]` filled `bg-accent`/`text-background` circle, `border-none`, `Plus size={16}` — replacing the old 22px outlined ghost circle (`border-border`/`bg-surface`/`text-text-muted`, `Plus size={13}`). Kept the existing idle/open opacity treatment (`opacity-35` at rest, `1` while open or hovered) rather than the header button's own hover-only opacity — the two reference buttons don't share an opacity model, so this is a deliberate merge of "add to story"'s fill/size with `BlockInserter`'s own existing affordance, not a straight copy of either. The wrapping row grew from `h-[22px]` to `h-[30px]` to avoid clipping the bigger button. Global change, **not** viewport-conditional — this file has no `isMobile`/`matchMedia` branching anywhere, confirmed by `BlockCanvas.test.tsx`'s own cross-viewport identical-className test. Unit-tested in `BlockCanvas.test.tsx` (per-block callback wiring, per-keystroke commit with no blur step, the N+1-inserter-slot count, the 2-option picker, the image-unavailable fallback, an explicit guard that no drag-handle markup ever renders, and the restyled trigger's className + its viewport-independence). |
| `GateView` | `components/shells/membership/GateView.tsx` | Rendered by `ChatHero` when `isGated`. Reads `hasInviteToken` and `invitedName` from `useChatStore`. **When signed in** (isLoaded + isSignedIn): "You're on the list." confirmation (fires `POST /api/heirloom/members/claim` once on the false→true sign-in transition). **When `!hasInviteToken`**: renders `<WaitlistView />` — email form that POSTs `{ email }` to `/api/heirloom/members/waitlist`; on success shows "You're on the list." copy. **When `hasInviteToken` but the token doesn't exist for this tenant at all** (garbage/unrelated string — `tokenExistsButUnauthorized` false): "By invitation only." / optional `invitedName` personalization + "Claim a free membership" button that opens the Clerk sign-up modal (unchanged legacy path — still reachable for this population only). **Component itself unchanged as of 2026-08-14, but this third branch is now unreachable for a genuinely-issued-but-invalid token** (`tokenExistsButUnauthorized` true, not signed in) — `isGated` is false for that case (see `chatStore` row above), so `ChatHero` renders the real chat instead of mounting `GateView` at all; see `System Docs/Marker Syntax.md`'s `[ACCOUNT_CREATE: expired invite]` entry for that replacement flow. `/api/heirloom/members/claim`'s only caller (the sign-in-transition effect in this component) is consequently now effectively orphaned — see `System Docs/Known Gaps.md`. Tailwind + Heirloom tokens. No Mantine. |
| `ChatHeader` | `components/shells/membership/ChatHeader.tsx` | "Your Story ⌄" is the conversation switcher (fixed 2026-07-30 — it previously had no `onClick` at all, a hardcoded label, and did nothing when tapped; on mobile it's the only visible way to switch conversations, since the docked `SidebarV2` Memories list is desktop-only). Label reflects `recentSessions.find(s => s.id === state.sessionId)?.title`, falling back to `"Your Story"` for a brand-new, not-yet-saved session. Clicking it opens a `recentSessions` dropdown (same list SidebarV2's Memories section reads); picking a row calls the shared `loadSession(id)`. Plus Account / Close `IconButton`s (Close dispatches `CLOSE_CHAT`). **Mount point changed 2026-08-08 (PR #302):** previously rendered nested inside `ChatHero`'s chat column — invisible before the memory panel existed (chat was the whole remaining width, so the header looked full-width by accident), visibly wrong once the panel became a sibling pane (the header only spanned the narrower chat column, not the panel beside it). Now a direct child of `ChatHero`'s root, above the sidebar + chat + panel row, matching the Story Canvas prototype `Design Handovers/design_handoff_memory_panel_layout_2026/README.md` cites as source of truth. `ChatHeader.tsx` itself is unchanged — this was a mount-point fix in `ChatHero.tsx` only. **Switcher gated off 2026-08-13** (`Design Handovers/ Aug 2026 Atomic Updates/Updated Headers`): a module-level `SHOW_STORY_SWITCHER` flag (default `false`) now wraps the entire switcher block above — state, handlers, and JSX are all still present and unchanged, just conditional on the flag, so flipping it back to `true` fully restores the old behavior with no other edits needed. When `false`, the same slot renders a static, non-interactive logo + "Legacy" wordmark instead — the real brand mark already defined in `LandingNav.tsx` (feather glyph in a rounded `bg-accent/20` square), scaled down from LandingNav's 64px marketing-bar proportions (`w-8 h-8` icon, `text-lg` wordmark) to this 48px in-app header's (`w-6 h-6` icon, `text-base` wordmark). Losing the switcher also removes the only mobile-visible way to switch conversations outside the hamburger drawer — desktop is unaffected, the docked `SidebarV2` Memories list still covers switching there. The `chatStore.loadSession.test.tsx` describe block exercising the dropdown is `describe.skip`'d, not deleted, for the same reason. **New icon, 2026-08-13 (Stage 1, `media_stages_08_2026`):** an optional `onOpenMedia` prop, when provided, renders an `Images`-icon `IconButton` ("Media from this chat") in the icon cluster, left of Share Heirloom. Opens `MediaGallery`, the in-chat panel scoped to the active session — this icon didn't exist before this stage; it was added because `SidebarV2`'s "Media" row, the only previous door to `MediaGallery`, was repointed at the new standalone `MediaPage` instead (see the `SidebarV2` and `MediaGallery`/`MediaPage` rows). `ChatHero.tsx` passes `onOpenMedia={handleOpenMedia}`. **New icon, 2026-08-14 (PR #385):** a second optional prop, `onOpenSessionMemories`, renders a `Bookmark`-icon `IconButton` ("Memories from this chat") right after the Media icon, same optional-prop-gated/spacing pattern — this didn't exist before this PR either; see the `ChatHero` row above (Session memories panel paragraph) and `System Docs/Known Gaps.md`'s Memories entry for why it was added and what it opens. `ChatHero.tsx` passes `onOpenSessionMemories={handleOpenSessionMemories}`. **Share Heirloom made conditional + wired, 2026-08-15 (wire-share-heirloom-modal):** `onShareHeirloom` is a third optional prop of exactly the same shape as the two above, and the `Share2` icon now follows the same `{prop && (…)}` gating they do. It did **not** before — it was the one always-rendered icon in the cluster, carrying `aria-disabled="true"` and no `onClick` at all, per its own "action stubbed; no share backend/modal mounted yet" comment. That makes the two "left of Share Heirloom" / "right after the Media icon" positions above slightly conditional now: with no handler supplied the Share icon doesn't render and the cluster closes up, exactly as it already does for Media/Memories. `ChatHero.tsx` passes `onShareHeirloom={() => setShareHeirloomOpen(true)}`, the same setter its `SidebarV2` call sites use — one modal, two doors (see the `ShareHeirloomModal` row below). |
| `ChatInput` | `components/shells/membership/ChatInput.tsx` | **Stale as of this pass — previously documented as a bare textarea; it is now a composer with file attachments + voice capture (795 lines).** Auto-growing textarea (`autoResize`, capped at 200px), Enter-to-send (Shift+Enter newline). `canSend` gates on trimmed text OR a pending attachment, and not `state.isLoading`/`isUploading`. Button cluster: Mic (`isMember`-gated — a `Lock` icon + "Voice is a member feature" for non-members), then `ArrowUp` Send / `Square` Stop swapped by `state.isLoading` (Stop calls the store's `stop`). **Attachments:** the "+" opens `SourceMenu.tsx` (own file, undocumented elsewhere in this table) — Take a photo / Photo library / Scan a document / Record audio / Browse files — wired to hidden `<input type="file">`s; client-side 50MB cap and HEIC rejection (console-warned + skipped, server also rejects if one slips through), `DEFAULT_FILE_ACCEPT` allowlist. Picked images are decoded for `width`/`height` off the local blob URL (feeds `UploadThumbnail`'s/`PendingEchoAttachment`'s real-aspect-ratio sizing, see those rows above). On send, each attachment uploads via `useMediaUpload` (`services/media/useMediaUpload.ts`); success appends a `[MEDIA_UPLOAD: filename | mediaItemId | type]` marker (or `[MEDIA_UPLOAD_DUPLICATE: ...]` on a content-hash dedup match) ahead of the trimmed text, a failure appends `[MEDIA_UPLOAD_FAILED: filename]` instead — the text turn still sends either way. `setPendingEcho`/`addMediaItem` (`chatStore.tsx`) drive the optimistic pre-send preview described in `MessageList.tsx`'s row above. **Voice:** Mic click starts an inline `MediaRecorder` bar (elapsed-time clock, `getUserMedia`); an Expand control promotes it to the full-screen `VoiceImmersive` surface (own file, undocumented elsewhere in this table), portaled via `useChatOverlayHost()` into `ChatDrawerV2`'s relative body — same portal mechanism `ImageLightbox` uses and for the same reason (own row above). No AI disclaimer. |
| `MessageList` | `components/shells/membership/MessageList.tsx` | Renders messages (assistant = `Bot`-icon avatar + left bubble, user = right bubble, no avatar) and a bouncing-dots typing indicator while `isLoading`; auto-scrolls to bottom, plus a scroll-to-latest nudge once scrolled away from it (own row below). **Scrollable container missing `min-h-0` — fixed 2026-08-06, PR #290.** The scroll container — originally a single `<div className="flex-1 overflow-y-auto ...">` (restructured since, see below) — was missing `min-h-0` — a flex child's default `min-height` is `auto`, which stops it shrinking below its own content's natural size even with `flex-1` set, so a long enough conversation overflowed past the composer sitting below it instead of scrolling within its own space. jefflougheed's equivalent container (`WidgetShellChat` row below: `flex min-h-0 flex-1 flex-col overflow-y-auto overscroll-contain`) already had it and never exhibited the bug. Fixed with a single class addition (`flex-1 min-h-0 overflow-y-auto overscroll-contain px-4 py-6`); the full ancestor flex chain was traced and confirmed already correct at every other level — `ChatDrawerV2`'s body div, and every wrapper between it and this container in `ChatHero.tsx` (line numbers not cited here or below — `ChatHero.tsx` was restructured 2026-08-08, PR #302, and this container itself restructured again the same day, PR #310, so exact line references would just go stale again; see the `ChatHero`/`ChatDrawerV2` rows above for the current structure) — this was the only gap. **Restructured again 2026-08-08, PR #310, to host the scroll-to-latest button (own row below):** the single div above is now an outer `relative flex-1 min-h-0` wrapper around an inner `absolute inset-0 overflow-y-auto overscroll-contain px-4 py-6` div — the split is what lets `ScrollToLatestButton` render as an absolutely-positioned sibling over the scrollable content instead of inside it. Same effective sizing behavior as before (the outer div is now the flex item claiming `flex-1 min-h-0`; the inner one fills it via `inset-0`), just two nested elements instead of one. **Visitor bubble shape (per `Design Handovers/spec_visitor_bubble.md`, 2026-07-28; widened + alignment-fixed same day):** `w-fit max-w-[90%]` shrink-to-fit measure (was `max-w-[75%]` with no `w-fit` — the bug that made short messages wrap early / long ones fail to respect the measure — then `76%` per the spec, then widened to `90%` after the 76% measure read as cramped next to the assistant's full-width response), `rounded-[18px] rounded-br-[5px]` (18px radius, 5px tail — was `rounded-2xl`/`rounded-br-sm`, 16px/2px), `border border-border` now applied unconditionally (previously only on the `failed` state — the sent-state bubble had no border at all), text at 15.5px/1.62 (was `text-base`/`leading-relaxed`, 16px/1.625), failed state is `bg-red-400/10 border-red-400/45` (was border-color-only, no background wash), inter-message gap `gap-5` (20px, was `gap-6`/24px), bubble→action-row gap `gap-1.5` (6px, was `gap-1`/4px). `bg-surface`/`border-border` are left as the plain existing tokens (not overridden to the spec's literal `#FFFDF9`/`#E8E0D2` hex) — the delta from the current `#FFFFFF`/alpha-hairline values is negligible, and `border-border`'s own code comment ("matches production weight") signals its alpha was already deliberately calibrated. **Bubble/action-row alignment:** `MessageBubble`'s user-message return is `group flex flex-col items-end gap-1.5` — Flexbox `align-items: flex-end` right-aligns each child (bubble, `DeliveryStatus`, `UserMessageActions`) to the container's own edge independently of its own width, so the action row's right edge always lands under the bubble's. A same-day CSS Grid attempt at this (`group grid justify-end gap-1.5`, with `DeliveryStatus`/`UserMessageActions` wrapped in `w-full min-w-0` divs) was reverted: CSS Grid's default `justify-self: stretch` stretches every item to a single shared column, and that column is sized to the WIDEST row's max-content — usually the actions row, not the bubble — so the bubble's own `max-w-[90%]` resolved against the actions row's width instead of the real container. Measured via a Playwright/Chromium harness reproducing the exact DOM+CSS: for a short message the two rows' right edges were ~136px apart under the grid version (vs. 0px under this flex version), and the bubble's *effective* max-width was bottlenecked to the actions row's own ~185px natural width rather than 90% of the container — explaining why the widen-to-90% change looked like it wasn't taking effect. (The assistant-message branch is early-returned dead code, per the file's own note above, and keeps the older `flex flex-col items-start` shape untouched.) **Assistant action-row alignment (found in production 2026-07-28):** `makeRenderAssistantMessage`'s `MessageActions` row sits below `AssistantMarkdownBubble` in a `ml-[60px]` wrapper — was `ml-11` (44px), which only accounted for the avatar (`w-8` = 32px) + `gap-3` (12px) and ignored the text bubble's own `px-4` (16px) left padding, so the action icons sat visibly left of where the actual prose text started (confirmed against a production screenshot, and measured via a Playwright harness: 16px delta with `ml-11`, 0px with `ml-[60px]`). jefflougheed's equivalent (`WidgetShell.tsx`'s `makeRenderAssistantMessage`) has no avatar to offset for — `SageReply`'s prose and its `MessageActions` row both use a single shared `pl-4`, so they align by construction and never had this bug. The `debugMarkers` wrapper below (admin-only) uses the same `ml-[60px]` for the same reason. Assistant prose runs through the marker registry (`createDefaultRegistry()`), so `[BOOKING:]` / `[NAME:]` / `[EMAIL:]` markers are stripped rather than shown as raw text (Heirloom has no booking-card UI, so BOOKING cards are dropped — only prose shows). Empty assistant bubbles (marker-only or cleared on error) are skipped, and the engine's `errorType` renders an on-brand error bubble (copy per type from `components/chat/errorCopy.ts`) with a real Retry button wired to `retry()`. **Admin debug view**: when `useAuthUser().user.isPlatformAdmin` is true (the client-side boundary signal, mapped from Clerk publicMetadata — display-only gating), all parsed markers extracted by the registry (`result.markers`) render as `DebugPill` components below each assistant message — dark `bg-black/60` monospace pill with a `debug` eyebrow label and the raw bracket text. Display-only: zero changes to the parser, registry, or store. `[SYSTEM: ...]` user-message detection is also wired but won't fire in practice since `sendHidden` never adds hidden turns to the store. Non-admin members: zero behavioral or visual change. **Visitor message actions (added 2026-07-27):** each user message renders `components/chat/UserMessageActions.tsx` (Edit/Copy/Send again — mutually exclusive with `DeliveryStatus`, and only shown once `status` is `'sent'`) below its bubble; tapping Edit swaps the bubble in place for `components/chat/EditableUserBubble.tsx` (an auto-growing textarea, Enter saves/Shift+Enter newline/Esc cancels) via local `editingId` state. Save calls `editMessage(id, text)`, Send again calls `resendMessage(id)` — both from `useChatStore()` (see `chatStore.tsx` above), which truncate the transcript forward from that message and re-deliver (see `useChatTurn.ts`'s `truncateAndRedeliver`). **Inline uploads (`UploadThumbnail`/`ImageLightbox`, replaced the card system — PR #293, 2026-08-06):** each `userMsg.uploads` entry renders via `UploadThumbnail` (own row below) instead of the deleted `UploadCard`/`UploadRunningCard`/`UploadReadyCard`/`UploadErrorCard` family; tapping a ready image opens `ImageLightbox` (own row below). A client-side pre-upload failure (no `media_items` row ever existed) still renders the pre-existing `FailedUploadChip`, unaffected by any of this. **Optimistic-send echo (`pendingEcho`, same PR):** on Send with an attachment, `ChatInput.tsx` sets `pendingEcho` (`chatStore.tsx`) before the upload round trip starts, so a local-preview placeholder appears immediately in the position the real message will land — the real message (and `UploadThumbnail`) doesn't exist yet at that point, since `handleSend` still gates `sendMessage` on the full upload resolving. `pendingEcho` is cleared the line after `sendMessage` fires, which is the same tick the real message is added (`useChatTurn`'s `send()` calls `addMessage` synchronously before its first `await`) — no visible gap or flash at the swap. Rendered by `MessageList.tsx`'s own `PendingEchoAttachment`/`PendingEchoBubble` (standalone, not built by reusing `UploadThumbnail`). Each `echo.attachments[]` entry also carries `width`/`height` (2026-08-07, client-captured in `ChatInput.tsx`) so `PendingEchoAttachment` sizes its own box with the same real-aspect-ratio math `UploadThumbnail` uses (own row above) — `PendingEchoAttachment` never reaches a `ready` state (no `status` concept at all; it's torn down the instant the real message exists, which only happens after uploads have already resolved), so it has no equivalent of the ready-checkmark badge. See `System Docs/Utilities/Chat UI.md`'s "Optimistic-send echo" section for the state mechanics and why it has no interaction with the `chat_id` backfill fix (PR #291); regression coverage in `components/shells/membership/optimistic-echo.test.tsx`. |
| `MagicLinkCard` | `components/shells/membership/MagicLinkCard.tsx` | Renders inline (below the assistant message that carried an `[ACCOUNT_CREATE: reason]` marker — see `System Docs/Marker Syntax.md`) as the email/phone OTP account-creation-or-sign-in card. Props: `reason?` (marker's free text, muted subheading), `initialName?`/`initialEmail?`/`initialPhone?` (pre-fill from `[NAME:]`/`[EMAIL:]`/`[PHONE:]` markers, falling back to `invitedName`/`invitedEmail`/`invitedPhone` off `chatStore`'s context when no marker has fired yet in the conversation — added 2026-08-14, PR #372; `MessageList.tsx` computes each as `?.fields[0] ?? invited* ?? null`, so a marker that does fire mid-conversation still wins, since it's checked first. No fallback on the story-invite path — a story invite link carries no specific invitee's contact info to fall back to), `inviteToken?`/`storyInviteToken?` (both raw tokens straight from `chatStore`'s context — see that row above — passed through unchanged, no transformation here), `onSuccess(name)`. Delegates all provider mechanics to `useAuthFlow()` (`services/auth/useAuthFlow.ts`) — this component owns only form state (name/tab/input/OTP values, resend cooldown) and calls `flow.sendEmail(value, name, inviteToken, storyInviteToken)` / `flow.sendPhone(...)` with the same four args, then `flow.verifyOtp(code)`. **Both tokens are written to Clerk `unsafeMetadata` in a single combined `signUp.update()` call** (`heirloom_invite_token`/`heirloom_story_invite_token`) by the Clerk adapter beneath `useAuthFlow` — see `System Docs/Utilities/Auth.md` for the mechanism and why it must be one call, not two. Stage machine (`useAuthFlow`'s `AuthFlowStage`): `idle → sending → otp_input → verifying → success`, with `expired`/`error` branches; `resend()` re-sends the same name + both tokens. On success, calls the `onSuccess` prop, which `MessageList.tsx` wires to `chatStore.tsx`'s `claimCurrentSession()` (the `[ACCOUNT_CREATE:]`/`handleAuthSuccess` chain — see the `chatStore` row above). |
| `ScrollToLatestButton` (`useScrollAnchor` hook + button) | `components/shells/membership/ScrollToLatestButton.tsx` | Floating "jump to newest message" nudge over `MessageList`'s transcript (2026-08-08, PR #310) — a chevron-down button, bottom-center, that appears once the visitor scrolls more than 48px from the bottom and smooth-scrolls back down on click. `useScrollAnchor(threshold = 48)` returns `{ ref, atBottom, handleScroll, scrollToBottom }`: `ref`/`onScroll={handleScroll}` go on the actual scroll container, `atBottom` (`scrollHeight - scrollTop - clientHeight < threshold`) drives the button's `visible` prop (`!atBottom`), and `scrollToBottom` (`ref.current.scrollTo({ top: scrollHeight, behavior: 'smooth' })`) is the button's `onClick`. Wired into `MessageList.tsx`'s transcript container (own row above) — the scroll div is `absolute inset-0` inside a `relative flex-1 min-h-0` wrapper specifically so the button can render as a sibling positioned over it, per the file's own usage comment. **Deliberately drops one piece of the reference file it was adapted from** (`ScrollToLatestButton.tsx` in `Design Handovers/design_handoff_memory_canvas_08_2026/`, explicitly "not memory-specific"): the reference's own `useEffect(() => { if (atBottom) scrollToBottom() })` — no dependency array, so it re-fires on every render — force-pins the scroll position on new content. `components/chat/ChatThread.tsx` already owns exactly that job for this same transcript (its own near-bottom tracking plus `scrollIntoView` keyed to `scrollDeps` — see that row above); transplanting a second copy would have both duplicated it and, worse, actually misbehaved here, since `MessageList.tsx` re-renders often for reasons that have nothing to do with new messages (feedback state, memory state, edit state), and each one would have re-triggered a smooth-scroll call. This hook is scoped to only what `ChatThread.tsx` doesn't already do: tracking scroll distance for the button's own visibility, and the button's explicit click-to-scroll. **Known interaction, not yet reconciled:** this button's 48px visibility threshold and `ChatThread.tsx`'s own 100px auto-follow band are two independent constants that can disagree in the 48–100px window — see `System Docs/Known Gaps.md`. Unit-tested in `ScrollToLatestButton.test.tsx` (happy-dom has no real layout engine, so `scrollHeight`/`scrollTop`/`clientHeight`/`scrollTo` are stubbed directly on the container node in every test). |
| `UploadThumbnail` | `components/shells/membership/UploadThumbnail.tsx` | Inline rendering for every attachment type (image/audio/document) — replaced the card-based system (`UploadCard`/`UploadRunningCard`/`UploadReadyCard`/`UploadErrorCard`, deleted PR #293, 2026-08-06) after a flicker bug was root-caused: the card selector returned three structurally different component types at the same JSX position based on `item.status`, and React's reconciliation is type-based at each tree position — when the returned type changes, React unmounts the whole old subtree and mounts a new one from scratch, regardless of a stable outer `key`. `UploadThumbnail` instead keeps one persistent element (an `<img>` for images, a static icon for audio/document) at the same JSX position and type across every status; the shimmer overlay (`animate-upload-shimmer`, reused from the deleted cards' Tailwind keyframes) and the failure retry badge are additive sibling children, conditionally mounted, never a type-changing wrapper — even the `<img>` itself never disappears, falling back to a 1×1 transparent-pixel data URI until a real `src` resolves. Image `src` prefers `item.localPreviewUrl` (an instant local blob URL, set by `ChatInput.tsx`) over `item.url` (the 60s-expiry signed URL from `GET /api/media`) — same fallback order the deleted `InlineImage` used. **Fresh-url re-resolution on the `item.url` fallback path (2026-08-09):** when `localPreviewUrl` isn't available — a page reload, or loading a session's history where no local blob ever existed — `item.url`'s 60s expiry would otherwise go stale mid-view, so that path renders through `useFreshImageUrl` (`services/media/useFreshImageUrl.ts`), the same hook the memory panel's `BlockCanvas.tsx`/`ImageBlockRow` uses, re-resolving the url via `GET /api/media/[id]/url` at display time while showing the stale value as an immediate fallback. The hook is only given a `mediaItemId` when `localPreviewUrl` is absent, so a live local blob never triggers the extra fetch — see `System Docs/Known Gaps.md`'s now-resolved chat-transcript-images entry. **Real aspect ratio, not a fixed crop (2026-08-07):** the image box used to be a hardcoded `h-36 w-48 object-cover` (192×144px), cropping the top/bottom off any portrait photo. It's now `max-w-60 max-h-80 object-contain` (240×320px bounding box, never upscaled past natural size) sized via an inline `style.width`/`height` computed by a local `thumbnailBoxSize()` helper from `item.width`/`item.height` — client-captured only (`ChatInput.tsx`'s `addFiles` decodes a picked image's `naturalWidth`/`naturalHeight` off the same blob URL already used for the preview, via a plain `new Image()` — no network round trip), threaded through `Attachment` → `ChatInput.tsx`'s `setPendingEcho`/`addMediaItem` calls → `PendingEcho.attachments[]` / `ClientMediaItem` (`chatStore.tsx`), and preserved across Realtime/catch-up merges by `mediaItemMerge.ts`. **Not persisted server-side** — `media_items` has no `width`/`height` column (see `System Docs/Database Schema.md`'s row and `System Docs/Known Gaps.md`) — so a historical/reloaded item falls back to `item.width`/`height` being `undefined`, and the `<img>` renders with no forced inline size, just the `max-w-60`/`max-h-80`/`object-contain` clamp (still no cropping, just without the zero-layout-shift guarantee the client-captured case gets). While `isUploading` and dimensions aren't yet known (a narrow edge case — send fired before the pick-time decode resolved), the box falls back to the old fixed 192×144px placeholder rather than collapsing to nothing, so the shimmer below still has a box to sit over. `MessageList.tsx`'s `PendingEchoAttachment` (own note in the row above) applies the identical `thumbnailBoxSize()` math to the pre-send echo — duplicated, not imported, per that component's own no-shared-code convention. A failed item shows a small retry badge (bottom-right corner, `bg-red-400`) with the sanitized (never raw) `error_message` as its label, calling the real `POST /api/media/[id]/retry`. **Persistent ready checkmark (2026-08-07):** once `item.status === 'ready'`, a small `bg-green-400` checkmark badge (bottom-left corner — the retry badge's mirror image, opposite corner) stays visible for as long as the thumbnail renders in that status — no timer/fade. Non-interactive (`<span aria-hidden>` with `data-testid="ready-checkmark"`, not a `<button>` — it has no action, unlike retry) and structurally independent from the failure badge: a separate `isReady` conditional, no shared state: the two badges are mutually exclusive by construction (a status can't be both `ready` and `failed`), never by an explicit shared guard. No brand-specific success/positive design token exists for Heirloom (checked `System Docs/Design System.md` — the only "Positive" token belongs to the isolated SBL palette), so `green-400` mirrors the retry badge's own raw-Tailwind-shade convention (`red-400`) rather than inventing one. No-remount regression test (seeds the same item id at `processing` then `ready`, asserts the underlying `<img>` DOM node reference is unchanged, and that the ready-checkmark only appears after the transition): `components/shells/membership/upload-thumbnail-render.test.tsx` — same file also covers the `useFreshImageUrl` wiring above (`localPreviewUrl` present → no fetch; absent → `<img src>` swaps from the stale `item.url` to the freshly-fetched one, mirroring `BlockCanvas.test.tsx`'s coverage of the same hook). Box-sizing math (portrait/landscape/square/small/unknown-dimensions cases) covered separately in `components/shells/membership/upload-thumbnail-aspect-ratio.test.tsx`, since jsdom has no real layout engine to assert actual rendered geometry against. |
| `ImageLightbox` | `components/shells/membership/ImageLightbox.tsx` | Tapping a ready image thumbnail (`UploadThumbnail`, row above) opens it near-full-screen — filename shown at the bottom, dismissible via backdrop tap, an explicit close button, or Escape. **Tap-based dismissal only** — no swipe gesture — a confirmed decision; there is no swipe-gesture precedent anywhere in this codebase. Portaled into `ChatDrawerV2`'s relative body via `useChatOverlayHost()`, the same mechanism `VoiceImmersive` uses and for the same reason: the drawer's CSS-transform slide animation would trap a `position: fixed` descendant, so the overlay is `absolute inset-0` scoped to the drawer's own relative body instead. Uses `useModalA11y` (row below) for Escape/focus-trap/focus-restore, same as the V2 modals. |
| Memory bookmark (`MemoryCard`, `MemoryRunningPill`, `MemorySavedReceipt`, `MemoryErrorLine`) | `components/shells/membership/memory/MemoryCard.tsx` | Rendered inline below the message it followed (not in its own transcript slot) by `MessageList.tsx`'s `renderMemorySlot`, driven by `services/chat/ui/v1/useMemories.ts` (see `System Docs/Utilities/Chat UI.md`'s row for the hook's own mechanics). The bookmark button itself is the `Bookmark` icon in `components/chat/MessageActions.tsx` / `UserMessageActions.tsx`'s action row (`onKeep`, `hasMemory`) — clicking it (or a guide-emitted `[SAVE_MEMORY]` marker, `System Docs/Marker Syntax.md`) calls `memories.create(anchorId, sourceKind)`. Four states below that row track the anchor's own memory: `MemoryRunningPill` (pulsing kind icon while `create()` is in flight — not cancellable), `MemoryCard` (the full draft — title/passage/Keep/Rewrite¹/Discard, `status: 'draft'`), `MemorySavedReceipt` (a slim "Kept" row once published), or `MemoryErrorLine` (a classified failure line with a "Try again" button — see below). ¹Rewrite is unwired to a local stub (fires a toast, does nothing); the archivist model call that used to power it was removed, so today the only way to get a different passage is Discard + bookmark again. **Per-anchor suppression (fixed 2026-08-06, PR #288):** the bookmark's `disabled` state (`keepDisabled`, computed per-row in `MessageList.tsx`) is true while streaming OR while `memories.hasOpenDraft(anchorId)` is true — previously `hasOpenDraft` was a single whole-session boolean, so one open draft anywhere in the transcript disabled every bookmark, including retries of a failed one (the regression `Design Handovers/heirloom_chat_handoffV2_2026_July 28/memories/README.md` §6 rule #3 warned against); now a stale/open draft on a different message never blocks this one, while a genuinely open draft on the *same* anchor still correctly disables its own bookmark. **Classified errors + retry (also fixed 2026-08-06):** `MemoryErrorLine` used to show one hardcoded string for every failure; it now takes `{ errorType, onRetry }` and renders the real `ERROR_COPY[errorType]` (`components/chat/errorCopy.ts`) — distinct copy for a network throw, a 401 "account required" rejection (anonymous or not-yet-linked visitor, see `Utilities/CRM.md`), a 5xx write failure, or a malformed response — with a working "Try again" button that re-invokes `create()` for that exact anchor. **`MemorySavedReceipt` icon + interaction (fixed 2026-08-08, PR #301 then PR #302):** the accent-soft circle shows the memory's own kind-specific icon (`KIND_ICONS[kind.icon]`, matching the running pill and draft-card header) at 12px, not the fixed checkmark it originally shipped with; the small glyph next to "Kept" is a `Check` — a distinct job from the kind icon above it (confirms saved, doesn't repeat what kind it is). The hover-revealed inline rename (pencil icon + input) was removed entirely — read-only, no edit affordance, per the diff audit against `Design Handovers/design_handoff_memories_08_2026/README.md` §5. The whole row is now a button (`onOpen`, optional prop, PR #302) that opens the memory in the memory panel — see the `ChatHero` row above and `System Docs/Known Gaps.md`'s Memories entry for what that panel currently does and doesn't do. **Real photo thumbnail, Photo Bookmark feature (shipped 2026-08-08, same night as the PR #302 fixes above):** both `MemoryCard`'s draft state and `MemorySavedReceipt` now render the actual linked photo — a 34px (`MemorySavedReceipt`) real `<img>`, `object-cover`, in place of the icon circle — whenever `memory.media_item_id` resolves against `sessionImages` (`memory.media_item_id ? sessionImages.find(img => img.id === memory.media_item_id) : undefined`, identical pattern to `MemoryCardView`/`BlockCanvas`'s own lookup, own rows below); a miss or an absent `media_item_id` (any non-photo memory) falls back to exactly the same icon-circle rendering as before, never a broken `<img>`. `MemorySavedReceipt` also gained its own "+" (add to a story) button — stubbed at first ship (same "Coming soon" toast `MemoryCardView`'s header "+" used to fire before PR #377 wired that one to a real `StoryPicker`), then wired for real itself (2026-08-14, memory-receipt-story-picker): it renders the exact same `StoryPicker` component `MemoryCardView`'s header "+" uses (see the `MemoryCardView` row above) — same backdrop/Escape popover mechanics, same `stories: Story[]`/`onAssignStory` props, current story highlighted with a checkmark, re-picking a no-op. `stories`/`onAssignStory` thread down from `ChatHero.tsx` through `MessageList.tsx` (`MessageListProps`/`MemorySlotHandlers`) the same way `sessionImages` already does — no second `useMemories` instance was needed the way `StoryMemoryEditor.tsx` (own row above) needs its own: this receipt only ever renders inside the currently-open chat's own transcript, so every memory it shows already belongs to `ChatHero`'s own `useMemories(state.sessionId)` instance, the one `handleAssignMemoryToStory` already closes over. A `stopPropagation` wrapper around the "+" keeps a story pick from also bubbling into the row's own `onOpen` click. **The trigger itself now also reflects assignment state and supports removal (remove-memory-from-story, 2026-08-14, threaded in on rebase — see `System Docs/Known Gaps.md`'s "Remove-from-story + trigger assignment state" entry for the fourth-caller correction this required):** since it's the same `StoryPicker` component `MemoryCardView`'s header uses, the trigger automatically shows the green checkmark state and, once a story is assigned, a "Remove from '[Story]'" popover item — no separate visual wiring needed. `onRemoveFromStory` threads down the identical path `onAssignStory` already does (`ChatHero.tsx` → `MessageList.tsx` → `MemoryCard.tsx`, `MemorySavedReceiptProps` defaulting it to a no-op rather than leaving it `undefined`, since `StoryPicker`'s own `onRemove` is required), sharing `ChatHero.tsx`'s existing `handleRemoveMemoryFromStory` as-is. See the `PhotoUploadActions` row directly below for the companion per-photo bookmark control this same feature added in the transcript. |
| `PhotoUploadActions` | `components/shells/membership/memory/PhotoUploadActions.tsx` | Photo Bookmark feature (shipped 2026-08-08) — an action row rendered below every **ready** photo thumbnail in the transcript (`MessageList.tsx`'s upload map), right-aligned under the image, not an overlay on it (placement corrected mid-build from the original design reference to a second, more recent one — `chat-widget-canvas.jsx`'s `UploadThumb`). Bookmark + "+" are hover-gated on the whole thumbnail's hover state (the `group` ancestor lives in `MessageList.tsx`, wrapping both the thumbnail and this row) with an `[@media(hover:none)]` always-visible fallback for touch. **Bookmark** calls `memories.create(msg.id, 'photo', mediaItemId)` — the third argument is what routes the memory through the new `createPhotoMemoryFromMedia` creation path (titles/bodies the memory from the photo's own AI caption, `media_items.derived_content`) rather than the pre-existing whole-message bookmark (`createMemoryFromAnchor`, own row above), and is what populates `artifacts.media_item_id` (`System Docs/Database Schema.md`'s `artifacts` row). Filled-icon + disabled treatment once this exact photo already has a memory, same `hasMemory`/`keepDisabled` convention as `UserMessageActions.tsx`'s own bookmark. **"+" (add to a memory)** fires the shared "Coming soon" toast — no `photo_artifacts` write path exists yet (`System Docs/Known Gaps.md`). **GPS badge** — a `MapPin` icon, rendered first in DOM (so hover-opacity on the group never clips it) and never hover-gated, showing whenever `mediaItem.latitude`/`.longitude` are both real numbers (`gpsFound`, computed in `MessageList.tsx` from `services/media/processor.ts`'s `extractGpsCoordinates` — `System Docs/Database Schema.md`'s `media_items` row); absent for the common case of a photo with no GPS EXIF data. The old whole-message bookmark is deliberately **not** gated off on a photo message that also has caption text — both controls coexist by design (confirmed with Jeff); only the old one's marker-leak bug (fixed via the shared marker registry, `System Docs/Known Gaps.md`'s Photo Bookmark entry) needed correcting. |
| `Sidebar` (v1 — **superseded, unmounted**) | `components/shells/membership/Sidebar.tsx` | The pre-V2 sidebar, replaced by `SidebarV2` in ChatHero. Kept on disk pending preview verification of the V2 pass; delete in the cleanup commit. |
| `SidebarV2` | `components/shells/membership/v2/SidebarV2.tsx` | V2 sidebar (collapsible `w-12` ↔ `w-64` via a **local** `expanded` state, default `true`). Fixed 2026-07-30: this used to be driven by the shared shell reducer's `state.isSidebarExpanded` — the same flag ChatHero uses to decide whether the *mobile* overlay renders at all (initial value `false`). On desktop, where SidebarV2 always renders, that meant the sidebar started in the collapsed icon rail with the entire Memories/Conversations list hidden — clicking "a conversation" appeared to do nothing because no row was rendered to click. The local state decouples "does this instance show full labels/lists" from "is the mobile overlay open," and starts expanded so Recent conversations are visible and clickable immediately on both mobile and desktop. Top → bottom: a combined search + collapse header row (own paragraph below, **as of 2026-08-13**) · New Chat (store `newChat()`) · Share Heirloom (**live as of 2026-08-15** — opens `ShareHeirloomModal`, see that row below; the row's `onShareHeirloom?.()` call was always here, but a hardcoded `opacity-40 pointer-events-none` on its className made it inert regardless, and was swapped for the same `${onShareHeirloom ? '' : …}` handler-presence check the Media row uses) · collapsible Conversations (store `recentSessions` + `loadSession`, active session highlighted, "No memories yet" empty state, "No matches" instead when a search query filters everything out) · **anonymous sign-in nudge** (v1 parity, `state.isMember` gate) · Stories (collapsible, default open — **as of 2026-08-12** the separate "Create" row is gone; the header now carries a "+" create button next to a chevron collapse/expand toggle, same interaction convention as Conversations' own chevron; "+" is disabled when `storiesDisabled` **or** `onCreateStory` is absent, and also force-opens the list on click so a newly created story is never hidden behind a collapsed section; story rows with hover tooltip from `description`, optional per-row "start chat" icon + a per-row invite icon + kebab) · Writing Prompts. Per-row kebab `RowMenu` **portals to `document.body`** with fixed positioning captured from the kebab rect (the `overflow-hidden` aside clipped in-tree menus), flips above when out of room below, closes on outside click / scroll / resize / capture-phase Escape; renders only when `onRowAction` is provided — **as of 2026-08-03 (PR #247), `ChatHero.tsx` passes `onRowAction` on both desktop and mobile, so menus render for both row types**; conversation `star`/`rename`/`delete` call real endpoints, story rows and `moveToChapter`/`removeFromChapter` remain no-ops (see `System Docs/Known Gaps.md`). **Invite — a dedicated per-row icon, not a kebab item, as of 2026-08-10 (invites-collaboration-modal):** the story-agnostic header "Invite" button (`onInviteToStories`) and the kebab's buried "Invite collaborators" item (`RowAction: 'invite'`) were both removed — `RowAction` no longer has an `'invite'` member at all (see the `types` row below). Each story row now carries its own invite icon (`UserPlus`, same sibling-button/hover-reveal treatment as the "start chat" icon, wired via the new `onInviteStory?: (storyId: string) => void` prop), opening the now-mounted `InviteCollaboratorsModal` scoped to that row's story (see that modal's own row below and `System Docs/Known Gaps.md`). **`forceCollapsed` prop (added 2026-08-08, PR #302):** when true, renders the collapsed `w-12` rail regardless of the internal `expanded` state — a derived `isExpanded = forceCollapsed ? false : expanded` now drives every render decision that used to read `expanded` directly — and hides the collapse-toggle chevron entirely (nothing meaningful to toggle to while forced). Doesn't touch `expanded` itself, so the user's own preference is exactly what they left it at once whatever forced it collapses. Used by `ChatHero.tsx` to force the rail while the memory panel is open (see the `ChatHero` row above). **`onMedia` wired 2026-08-09:** the "Media" nav button (`Images` icon, `aria-label="Media"`, rendered `opacity-40 pointer-events-none` whenever `onMedia` is absent — this gating was already built, just never fed a real handler) previously always received `undefined` from both `ChatHero.tsx` call sites. The desktop call site originally passed `onMedia={handleOpenMedia}` and `forceCollapsed={!!openMemory || mediaOpen}` (rail also collapses while the media pane is open); the mobile call site passed `onMedia={() => { dispatch({ type: 'TOGGLE_SIDEBAR' }); handleOpenMedia(); }}`, closing the drawer before opening the mobile bottom sheet. No changes to `SidebarV2.tsx` itself were needed for that pass — both the button and the `onMedia` prop were already fully built. **Repointed 2026-08-13 (Stage 1, `media_stages_08_2026`):** both call sites now pass `onMedia={handleOpenMediaPage}` (desktop) / an inline handler wrapping it that still closes the drawer first (mobile) instead — the "Media" row now opens the new standalone `MediaPage` (own row above), not the in-chat `MediaGallery` panel. `forceCollapsed` is unchanged (`!!openMemory || mediaOpen` — the standalone page doesn't force the rail, since it's a full-screen overlay covering the sidebar regardless). The in-chat panel moved to a new `ChatHeader` icon instead (see that row above) — `SidebarV2.tsx` itself again needed no changes, only its `ChatHero.tsx` wiring did. **Search + collapse combined into one header row, search threshold removed, search wired to real filtering — 2026-08-13** (`Design Handovers/ Aug 2026 Atomic Updates/Search and Collapse Bar`, Phase 1; PR #363, commit `f6327ef` rebased onto the target-aware kebab work below before merge): the standalone collapse-toggle row and the separate threshold-gated `SearchField` row are now one row. `searchThreshold`/`searchRevealed` are gone from `SidebarV2Props` entirely — confirmed via grep that neither was ever passed from either `ChatHero.tsx` call site, so no upstream cleanup was needed — `SearchField` is now always rendered `revealed` (no conversation-count gate); the hover/focus/typing "active" pill-border treatment it already had is untouched. Search now actually filters — previously `SearchField` owned its own local `value` state and fired `onSearch` per keystroke into a prop nothing consumed. Query state now lives in `SidebarV2` itself (`const [query, setQuery] = useState('')`; `SearchField` is a controlled input reading `value` from it), and drives BOTH lists live off the one query, case-insensitive substring — Memories by `session.title`, Stories by `story.name`, simultaneously. **Memory content (passages) is explicitly out of scope for this phase** — no client-side data source spans memories across sessions yet (`useMemories(sessionId)`, own row above, only ever fetches one session's memories at `/api/sessions/${sessionId}/memories`); wiring that in is scoped as a separate, later handover. On mobile (`onClose` provided), the same slot that holds the collapse toggle on desktop instead renders a Close-X — reusing `ChatHeader.tsx`'s existing `X` icon + `IconButton` pattern for visual consistency (see the `ChatHeader` row above), wired to `SidebarV2`'s own `onClose` prop (already correctly wired to `TOGGLE_SIDEBAR` from both `ChatHero.tsx` call sites), not a new handler. `forceCollapsed` still hides the toggle/Close-X entirely, same as before this change (see the `forceCollapsed` paragraph above); `SearchField` still degrades to its icon-only button when collapsed, same as before — **though see `System Docs/Known Gaps.md` for a bug found on that collapsed icon-only search button.** **`onSelectStory` wired for real 2026-08-14 (real-story-view-1d-entry-point):** each story row's own `onClick={() => onSelectStory?.(story.id)}` was already built into this file — the prop just went unfed from both `ChatHero.tsx` call sites until this pass (a confirmed no-op, `System Docs/Known Gaps.md`). No `SidebarV2.tsx` changes were needed, same shape as the `onMedia` wiring history above — see the `ChatHero` row's Real Story View paragraphs for `handleSelectStory`'s branching logic. **Active-item-to-top (`active_item_to_top` handover):** Conversations first (2026-08-13) — `orderedSessions`, a `useMemo` derived from `recentSessions` + `state.sessionId`, moves the active session to index 0 (stable otherwise, no reorder of the rest); `filteredSessions` (the search+collapse redesign, merged the same day) filters `orderedSessions`, not `recentSessions` directly, so the active row stays pinned to the top under a live search too. A no-op — same array reference — when nothing's active or it's already first, so nothing downstream that relies on referential stability sees a needless new array. Stories got the same treatment a day later (2026-08-14, PR #390, closing the other half of the handover): `orderedStories`/`filteredStories` mirror `orderedSessions`/`filteredSessions` exactly, but keyed off a new `activeStoryId?: string` **prop** rather than store state — a story pane isn't `chatStore` state the way a session is, so `ChatHero.tsx` passes its own `storyViewId` in (`activeStoryId={storyViewId ?? undefined}`, both desktop and mobile call sites) instead of this component reading it off `useChatStore()` itself. **Row copy darkened to full-strength ink — 2026-08-15 (PR #397):** three groups moved from `text-text-muted` to `text-text-primary` (`#1F1A14` on Heirloom, ~16:1 against `--color-background` — the same ink body and heading text already use): the shared `navBtn` base (New Chat · Media · Share Heirloom · Memories — each row's icon is a `currentColor` child, so it darkens with its label), conversation-title rows in their **unselected** state (the selected state was already `text-text-primary`, so selected vs. unselected is now distinguished by the `bg-text-primary/10` background alone), and the story-row `<button>` wrapping the accent dot + `story.name` (the dot is its own span on `bg-accent/60` and is unaffected). Deliberately left muted: the `Stories`/`Writing Prompts` eyebrow labels and their icons, the writing-prompt cards, the "No memories yet"/"No matches" empty states, the anonymous sign-in nudge, both section chevrons, the hover-reveal action buttons (kebab/invite/start-chat), and the `SearchField` placeholder — and the global `--color-text-muted` token itself is untouched, since it is consumed sitewide well beyond this component. Three now-redundant hover classes went with the swap: `hover:text-text-primary` at all three sites (a no-op once the base is already primary), plus the story row's `disabled:hover:text-text-muted`, which existed only to cancel that hover-darken and would otherwise have made **disabled** story rows *lighten* on hover — disabled rows now stay primary at `opacity-40`, and `Share Heirloom` stayed recessed through its own `opacity-40` rather than through colour. **That last clause stopped being true hours later, same day:** wire-share-heirloom-modal made Share Heirloom's `opacity-40 pointer-events-none` conditional on `onShareHeirloom` being supplied (it always is, from both `ChatHero.tsx` call sites), so the row is no longer recessed at all — it now renders at the same full-strength `text-text-primary` as New Chat and Media, which is the correct resting state for a live control. Note that hover feedback on all three groups is consequently background-only now; see `System Docs/Known Gaps.md`. |
| `ChatDrawerV2` | `components/shells/membership/v2/ChatDrawerV2.tsx` | Right-anchored drawer wrapper: fixed, `z-50`, slide-in via translate-x, two width states (`defaultWidthClassName` ↔ `w-screen` when `isFullScreen`). `inert` + `aria-hidden` + `pointer-events-none` while closed (off-screen content leaves the tab order). Optional built-in minimal header (`showHeader`, default true) — Heirloom passes `false` and keeps `ChatHeader`. Body is `position: relative`, the containing block for the V2 modals' `absolute inset-0` overlays. **Width cap (relevant since 2026-08-08, PR #302):** `defaultWidthClassName` defaults to `w-[clamp(680px,50vw,1120px)]` — the drawer is capped between 680px and 1120px, never full viewport width, unless `isFullScreen`. Nothing in this component's own DOM (this wrapper or its body div) has `overflow-hidden`, so content nested inside that grows past this width (e.g. a flex row whose children's combined `min-width` exceeds it) renders past the drawer's right edge instead of being clipped or wrapping — confirmed as the root cause of a real overflow bug in the memory panel (`ChatHero` row above); the fix was `min-w-0` at every level between the overflowing row and this cap, not a change here. Worth knowing before adding any other wide content inside this drawer. |
| `BeginStoryModal` | `components/shells/membership/v2/BeginStoryModal.tsx` | "Begin a new story" modal (props only: `open` / `onClose` / `onCreate(name, description)`). Name required (Enter submits), description optional (becomes the story row tooltip). Mounted by ChatHero; `onCreate` appends to the ephemeral stories state. Uses `useModalA11y`. |
| `InviteCollaboratorsModal` | `components/shells/membership/v2/InviteCollaboratorsModal.tsx` | Mounted and wired 2026-08-10 (invites-collaboration-modal); its backing mechanism was replaced later the same day (reusable-story-invite-links) — originally a single-use `members`-row invite via `POST /api/heirloom/invites`, now the durable `story_invite_links` table via `POST /api/heirloom/story-invites` (`System Docs/Database Schema.md`, `System Docs/API Routes.md`'s "Story Invite Links" section), so many different people can accept the same link independently, not just one. Create/copy flow, invalidation warning, and real roster reworked later the same day again (invite_modal_updates, Phase 5 — see `System Docs/Known Gaps.md`). Opened from `SidebarV2`'s per-story invite icon (own row above); `ChatHero.tsx` owns `invite: { storyId } \| null` + `invitePrimer` + `inviteLink` + `inviteCollaborators` state. Layout is a fixed-height flex column — everything except the magic link (header, story picker, Custom Greeting, roster, disclaimer) scrolls in a body region; the magic-link block (link row, expiry, reset) is a non-scrolling footer pinned to the bottom. `stories: Story[]` / `storyId?: string` back the story picker — the intro copy's story name is derived internally (`stories.find(s => s.id === storyId)?.name`). `primer: string` / `onPrimerChange?` back **Custom Greeting**, the exact `members.primer` mechanism `InviteMemberModal.tsx`'s admin flow uses (same 500-char cap + live counter — unchanged by Phase 5, per that pass's own instruction not to touch it). **`magicLink?: string` is now optional** (was required) — opening the modal (`handleInviteStory`) no longer *creates* anything; it just sets `invite: { storyId }` with `inviteLink`/`invitePrimer` reset to `null`/`''` first. **Restored on open, 2026-08-11 (invite-modal-restore-on-open):** that reset used to be the end of it — reopening the modal for a story that already had a real active link and a saved primer still showed "Not created yet" and a blank field, since nothing fetched existing state. The collaborators-fetch effect (already keyed on `invite?.storyId`, so it fires on every open) now also reads `active_link` off the same `GET /api/heirloom/story-invites?story_id=` response (`getActiveStoryInviteLink`, read-only, no create-if-missing — `System Docs/API Routes.md`'s "Story Invite Links" section) and, when present, overwrites the blank reset with the real `token`/`primer`/`invite_url` a beat later. A story with no active link is unaffected — the blank reset simply stands. With no `magicLink`, the footer renders "Not created yet" + a **Create** button (`onCreateLink`, new prop) in the same slot Copy occupies once a link exists; expiry + "Reset link" (`onResetLink`, unchanged) render only once `magicLink` is set. Create and Reset both call `ChatHero.tsx`'s `createInviteLink`, just with `reset` false vs. true — Create uses the fetch-or-create path (safe against a story that already has an active link from another session) rather than blindly rotating. **Invalidation warning, genuinely new:** the modal owns its own `pendingEdit` state — `onStoryChange`/`onPrimerChange` from the picker/textarea go straight to the caller only when `!magicLink`; with a link live, the value is captured into `pendingEdit` instead and a warning dialog ("This will invalidate the current link…") gates it. Cancel discards `pendingEdit` untouched; Continue calls the real `onStoryChange`/`onPrimerChange`, which in `ChatHero.tsx` both applies the edit AND fires `DELETE /api/heirloom/story-invites` to revoke the now-stale link server-side (dropping `inviteLink` back to `null` locally too) — it does not auto-create a replacement. Reset link bypasses this warning entirely (fires directly), per the design reference's own reasoning that its label already says what it does. **Roster is real now:** relabeled "Existing members" (was "Already invited"), the joined/pending count and mixed badges are gone — every row reflects a genuine `artifact_subscribers` grant (`GET /api/heirloom/story-invites?story_id=`, `listStoryCollaborators`, `System Docs/API Routes.md`), refetched whenever the modal's `storyId` changes. Badge is "Joined `[date]`" — **no memory count**, unlike the design reference's mockup: nothing in this codebase counts memories per collaborator per story yet (blocked on `artifact_containments`, still unwired — see Known Gaps). `Collaborator.relationship` is now optional and only rendered when present, since the real `members` table has no relationship column to source it from. |
| `ShareHeirloomModal` | `components/shells/membership/v2/ShareHeirloomModal.tsx` | **Mounted and wired 2026-08-15** (wire-share-heirloom-modal) — was "landed but not mounted" until then. Still props-only (no store, no API): copy-link via the clipboard + X/Facebook/LinkedIn/Email share intents opened with `window.open`, `useModalA11y` for Escape/focus-trap/focus-restore. `ChatHero.tsx` owns a `shareHeirloomOpen` boolean and renders it at the same `z-[80]` modal layer as `BeginStoryModal`/`InviteCollaboratorsModal` — above every overlay it can coexist with (mobile sheets and `MediaPage` at `z-40`, the mobile sidebar at `z-30`); only `ConfirmDeleteModal` (`z-[85]`) and the toast (`z-[100]`) sit higher, and neither can be up at the same time. **Two entry points, one modal:** `ChatHeader`'s `Share2` icon (`onShareHeirloom`, rendered conditionally like `onOpenMedia`/`onOpenSessionMemories` — it was previously always rendered but `aria-disabled` with no `onClick`) and `SidebarV2`'s "Share Heirloom" nav row (whose `onShareHeirloom?.()` call was already built but sat behind a *hardcoded* `opacity-40 pointer-events-none`, so the handler could never fire until that was swapped for the same handler-presence check the "Media" row uses). The mobile sidebar call site closes the drawer on the way in, matching `onMedia`/`onSelectStory`. **Deliberately not part of the third-pane reciprocal-close wiring** — it's a centered modal over the whole drawer, not a pane competing for that slot. Default `shareUrl` is the real production domain with standard share UTMs (`https://heirloom.2bl.ai/?utm_source=share&utm_medium=social&utm_campaign=withlove`) — corrected 2026-08-15 from the `heirloom.life` placeholder. **No analytics tool reads those UTMs yet** (nothing in this codebase consumes them); they're on the URL so links shared before that lands stay attributable afterwards. One `utm_medium` across every channel is intentional — the campaign is "someone passed this on", not per-network attribution. An override to `shareUrl` replaces the whole string, UTMs included. Integration coverage in `components/shells/membership/ChatHero.shareHeirloom.test.tsx` (both entry points desktop + mobile, X/Escape/backdrop dismissal, clipboard copy, all four channel intents, and the default URL + each UTM param asserted against a literal spelled out in the test rather than imported from the component). |
| `useModalA11y` | `components/shells/membership/v2/useModalA11y.ts` | Shared modal behavior: capture-phase Escape with `stopPropagation` (one press closes one layer — the panel's window-level Escape handler never fires while a modal is open), initial focus into the dialog (`initialFocusRef` or the `tabIndex={-1}` container), focus restore on close, Tab/Shift+Tab focus trap. Unit-tested in `useModalA11y.test.tsx` (6 tests). |
| `types` (V2 domain) | `components/shells/membership/v2/types.ts` | `Story`, `Collaborator`, `WritingPrompt`, `RowTarget`, `RowAction` — extracted from `SidebarV2.tsx` at landing (see `v2/HANDOVER.md`). **`RowAction` dropped `'invite'` 2026-08-10** (invites-collaboration-modal) — it was a deliberate no-op buried in the kebab for both row types, superseded by `SidebarV2`'s dedicated per-story-row invite icon (`onInviteStory`), which isn't routed through `onRowAction`/`RowMenu` at all. **`RowAction` gained `'admin'` 2026-08-13** (Updated Story Kebabs handover) — story-only (gated via `MENU_ITEMS`' `targets`), opens `StoryAdminPanel` (`v2/StoryAdminPanel.tsx`, no dedicated `Public Site.md` row yet — see the `ChatHero` row above) via `ChatHero.tsx`'s `adminStoryId` state; that panel is real and built (description edit + member roster/remove), not a stub. Current members: `'star' \| 'rename' \| 'admin' \| 'moveToChapter' \| 'removeFromChapter' \| 'delete'`. **`Story` gained `hasActiveInviteOrSubscribers?: boolean` the same day (reusable-story-invite-links)** — from `GET`/`POST /api/stories` (`services/crm/stories.ts`'s `listStories`/`createStory`); consumed only by `ConfirmDeleteModal`'s extra delete-warning sentence, not used to gate the Invite/Delete controls themselves (deliberately left unrestricted for non-owner collaborators in V1 — see `System Docs/Known Gaps.md`). |
| `Avatar` / `IconButton` / `Button` | `components/shells/membership/ui/` | Tailwind UI primitives ported from the legacy repo, styled with the Heirloom tokens. |

Persistence note: `services/chat/ui/v1/persistence.ts` is the pure (no React,
no store dependency) **IndexedDB** buffer (via the `idb` package) behind the
durability behavior above. (Moved from `app/heirloom/lib/chatPersistence.ts`
in centralization Step B, and re-platformed from localStorage to IndexedDB
since — each namespace gets its own database, `heirloom:chat:v1` for Heirloom
and `sage:chat:v1` for jefflougheed, opened via `openDB`.)
Multi-thread by design — each database holds a `threads` object store keyed by
session id (or the `DRAFT_ID` sentinel for a thread with no server session
yet) and a lightweight `threadIndex` object store (`{ id, updatedAt, title? }`
per thread) that feeds the currently-empty Recent sidebar and lets
`findMostRecentThread` pick the newest entry by `updatedAt`. Two removal
helpers mirror each other: `clearDraft()` drops the draft-slot thread + its
index entry (called when a real session id arrives), and
`clearSession(namespace, sessionId)` drops a session-keyed thread + its index
entry (called by New Chat so the cleared conversation is not re-hydrated on
the next mount). Every entry carries an `updatedAt` ISO timestamp, so recovery
is "most-recent wins" (the chosen resolution for the eventual
DB-vs-IndexedDB conflict). The store converts between its in-memory `Message`
(`timestamp: Date`) and the module's JSON-serializable `PersistedMessage`
(`timestamp: ISO string`). SSR-safe (guards `typeof indexedDB`) and
best-effort (swallows quota/serialization errors, one open connection per
namespace memoized so repeated calls don't reopen the database). Unit-tested
in `services/chat/ui/v1/persistence.test.ts`. **This is IndexedDB only — the
session create/PATCH API and the DB write path are unchanged.**

Tenant note: `/api/sage` resolves the tenant from the host. **Resolved
2026-08-04** — Heirloom's tenant (`20767f1d-1148-4e43-ab73-f6da88f0ac56`) now
has its own live `compiled_prompts` row (`status='live'`, id
`7743fd18-37df-46b2-9036-6d0ef5dbf7ca`), confirmed via direct SQL. Heirloom
chat runs its own compiled prompt, not the shared `DEFAULT_SYSTEM_PROMPT`
fallback.

---
